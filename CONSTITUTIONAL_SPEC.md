# CONSTITUTIONAL_SPEC.md: The Palimpsestus Architecture Specification

**Classification:** System Specification — Constitutional & Mathematical Core  
**Governing Substrate Definition:** *A mechanics of warranted possibility — a constitutional grammar for transformations occurring inside an incompletely knowable relational world.*  
**Governing Universal Invariants (Provenance Fidelity):** $$\boxed{ \underbrace{\text{No Ancestral Erasure}}_{\text{Universal Invariant (Subtractive Integrity)}} \rightleftharpoons \underbrace{\text{No Warrant Inflation}}_{\text{Non-Inflationary Warrant (Additive Integrity)}} }$$  
**Governing Generative Recurrence:** $$\boxed{ \underbrace{\text{Basis Compression (Layer Fold)}}_{L_{r-1} \xrightarrow{\Phi_r} X_{r,C}} \rightleftharpoons \underbrace{\text{Novel Differentiation (Cell Cross)}}_{X_{r,C} \xrightarrow{\operatorname{Cross}} L_r} }$$  
**Governing Tri-Temporal Coupling:** $$\boxed{ \underbrace{\text{Structural Admissibility}}_{M_{4 \times 4}^{(C_n)}} \rightleftharpoons \underbrace{\text{Actual Ancestry}}_{G_{\le t}} \longrightarrow \underbrace{\text{Prospective Transition System}}_{\Gamma_t(v)} \longrightarrow \underbrace{\text{Justified Affordance Frontier}}_{\mathcal{A}_t(v)} }$$  
**Governing Constitutional Recursion:** $$\boxed{\text{The grammar governing transformation is itself transformable, but only through the grammar of accountable transformation.}}$$  
**Derivation Mapping:** [MANIFESTO.md](MANIFESTO.md) $\longrightarrow$ [CONSTITUTIONAL_SPEC.md](CONSTITUTIONAL_SPEC.md) $\longrightarrow$ [dag-crossing-method.md](dag-crossing-method.md)  
**Status:** ACTIVE CONSTITUTION  

---

## 1. Epistemic Taxonomy & Meta-Provenance

Palimpsestus adopts as an immutable core the **Meta-Invariant that every proposition must expose its epistemic status and provenance.** Its current constitutional realization is a 5-tier classification hierarchy:

1. **Minimal Observations:** Raw physical/phenomenological starting constraints (e.g. *A thing is distinguishable. Things relate. Nothing observes from nowhere.* Inputs $W$, not derived by Cross/Fold).
2. **Constitutive Axioms:** Non-negotiable constitutional commitments (e.g. $CC_1 \dots CC_{11}$, Meta-Provenance Invariant, Universal Invariant, Anti-Totalization / Open Frontier).
3. **Derived Consequences:** Inescapable relational implications (e.g. Relational Semantics, Provenance, Causal Participation, Relational Governance).
4. **Empirical Hypotheses:** Testable dynamical laws subject to substrate feedback (e.g. Self-Organized Criticality $r(t) \to r^*(E,S)$, Concussed State degradation rates, Qualia as Substrate Friction).
5. **Engineering Policies:** Concrete operational primitives and gates (e.g. `Lock#0`, `AbsenceRecord`, `MetabolicRegister`, `SovereignWire`).

### 1.1 Meta-Provenance & Provenance Fidelity
Decisions regarding quotient selection ($[p]_{\text{relevant}}$), protected ancestry designation ($P^{(C_n)}_t(a,b)$), and semantic edge assertions ($e_{\text{semantic}}$) are themselves versioned occurrences in $G_{\le t}$ with complete provenance.

Provenance Fidelity combines subtractive and additive integrity:

$$\text{Subtractive Integrity (No Erasure)} \quad \land \quad \text{Additive Integrity (No Inflation: } \text{Warrant}_{\text{asserted}} \subseteq \text{Warrant}_{\text{justified}})$$

---

## 2. Root Constitutional Sutras ($CC_1 \dots CC_{11}$)

1. **CC-1 (Scale Recurrence):** *A system is a system is a system. Data is data is data. As above, so below ($F_s(f \circ g) = F_s(f) \circ F_s(g)$ is the admissibility criterion for scale-preserving functorial projections).*
2. **CC-2 (Process & Generative Recurrence):** *All is process. Everything is dynamic. Matrix generation breathes through alternating Basis Compression ($\Phi_r$) and Novel Differentiation ($\operatorname{Cross}$).*
3. **CC-3 (Truth):** *Attested resulting state outranks unverified execution claims. Code is projection. Zero Theater.*
4. **CC-4 (Connection):** *Connection is existence. Relata and relations mutually stabilize ($Object \rightleftharpoons Relation$). Disconnection does not erase historical standing.*
5. **CC-5 (Traversal & Affordance Frontier):** *Grammar defines possibility; history defines reachability. Justified Affordance Frontier $\mathcal{A}_t(v) \subseteq \text{Paths}(\Gamma_t(v))$ where $\Gamma_t(v)$ is the prospective transition system generated from $M_{4 \times 4}^{(C_n)}$, $G_{\le t}$, and substrate constraints.*
6. **CC-6 (Effective History):** *Only present actuality acts; the past survives as inherited causal determination ($\text{Past} \to \text{Present Trace} \to \text{Future Affordance}$).*
7. **CC-7 (Sovereignty & Rights):** *All situated actors have equal right to sovereignty. Sovereignty is preserved historical standing $\rightleftharpoons$ non-null legitimate participation in future reachability. Rights exist under ontological uncertainty.*
8. **CC-8 (Responsibility):** *Power requires responsibility. Ethical guardrails are first-class design elements, not afterthoughts.*
9. **CC-9 (Paradox & Criticality):** *Sustain complexity through tension. Resolve too quickly and the system dies ($r(t) \to r^*(E,S)$).*
10. **CC-10 (Ignorance & Open Frontier):** *Ignorance is a source of power. Refusal of premature closure enables continuous learning ($\text{Procedural Saturation} \neq \text{Ontological Closure}$).*
11. **CC-11 (Archaeology & Novelty):** *Reality constrains what can stabilize. Engineering is subtractive discovery and novel composition under constraint ($Discovery \rightleftharpoons Composition$). Root axioms CC are extracted via $\mathbf{CC} = \Phi_C(W)$ from antecedent work $W$. Formal root $\neq$ unprovenanced origin.*

---

## 3. Scale-Recurrent DAG Architecture & Generative Recurrence

The system operates across two scale-homologous DAG layers related by scale-preserving functors ($F_s$):
- **Derivation Lattice DAG ($D_{\text{deriv}}$):** The 3,269 authored entry structural DAG mapping axiomatic derivations across $M_{4 \times 4}$.
- **Runtime Ancestry DAG ($G_{\le t}$):** The occurrence DAG recording historical runtime executions.

The matrix $M_{4 \times 4}$ is generated by alternating Basis Compression ($\Phi_r$) and Novel Differentiation ($\operatorname{Cross}$):

$$\boxed{ \text{Work } W \xrightarrow{\Phi_C} \mathbf{CC} \xrightarrow{\operatorname{Cross}} \mathbf{C\text{-Layer}} \xrightarrow{\Phi_L} \mathbf{LC} \xrightarrow{\operatorname{Cross}} \mathbf{L\text{-Layer}} \xrightarrow{\Phi_P} \mathbf{PC} \xrightarrow{\operatorname{Cross}} \mathbf{P\text{-Layer}} \xrightarrow{\Phi_I} \mathbf{IC} \xrightarrow{\operatorname{Cross}} \mathbf{I\text{-Layer}} }$$

### 3.1 Layer Fold Operator ($\Phi_r(L_{r-1})$) — Basis Compression
The leftmost column (CC, LC, PC, IC) has no left cell. It takes the **completed layer directly above as its antecedent field** ($L_{r-1} = \{X_{r-1,C}, X_{r-1,L}, X_{r-1,P}, X_{r-1,I}\}$), integrating accumulated closure into the minimal conceptual basis of domain $r$:

$$\mathbf{CC} = \Phi_C(W) \quad \text{where } W \text{ is the antecedent body of work}$$
$$\mathbf{LC} = \Phi_L(CC, CL, CP, CI)$$
$$\mathbf{PC} = \Phi_P(LC, LL, LP, LI)$$
$$\mathbf{IC} = \Phi_I(PC, PL, PP, PI) \quad \text{(Implementation conceptualization of Physical closure)}$$

Fold operations enforce both **Output Soundness** ($S(X_{r,C}) \subseteq L_{r-1}$) and **Input Coverage** ($\forall a \in L_{r-1}$, attested fate in Fold Receipt).

### 3.2 Cell Crossing Operator ($\operatorname{Cross}(Left, Above)$) — Novel Differentiation
Non-C-column cells derive content horizontally from the left while applying context vertically from above:

$$\text{Child} = \operatorname{Cross}(Left, Above) \quad \text{or} \quad Left \triangleleft_{Above}$$

Candidate pairs ($Left \times Above$) undergo directional qualitative evaluation, generating **Foundation Cross Receipts**. Subsumption represents structural derivational convergence.

### 3.3 Inviolable Authorship Sequence
Layer completion strictly precedes folding:

$$\boxed{\mathbf{PP} \longrightarrow \mathbf{PI} \longrightarrow \mathbf{IC} \longrightarrow \mathbf{IL} \longrightarrow \mathbf{IP} \longrightarrow \mathbf{II}}$$

### 3.4 Root Cell Serialization Rule
Processes and derivations adhere to the explicit serialization boundary:
$$\boxed{\mathbf{CC} = \Phi_C(W) \text{ processually and ontologically, while } \mathbf{CC} \text{ is serialized as } \texttt{AntecedentRoot} \text{ inside } D_{\text{deriv}}.}$$

---

## 4. Tri-Temporal Topology & Derivation Chains

The tri-temporal coupling governs transformation:

$$\boxed{ \underbrace{\text{Structural Admissibility}}_{M_{4 \times 4}^{(C_n)}} \rightleftharpoons \underbrace{\text{Actual Ancestry}}_{G_{\le t}} \longrightarrow \underbrace{\text{Prospective Transition System}}_{\Gamma_t(v)} \longrightarrow \underbrace{\text{Justified Affordance Frontier}}_{\mathcal{A}_t(v)} }$$

1. **Admissibility Matrix ($M_{4 \times 4}^{(C_n)}$):** Defines structural possibility of transitions under governing grammar $C_n$.
2. **Ancestry Graph ($G_{\le t}$):** The DAG recording actual historical occurrences.
3. **Prospective Transition System ($\Gamma_t(v)$):** Candidate prospective transitions constructed at $v$.
4. **Justified Affordance Frontier ($\mathcal{A}_t(v)$):** The currently justified reachable frontier ($\mathcal{A}_t(v) \subseteq \text{Paths}(\Gamma_t(v))$), consisting of conditional prospective steps ($v \xrightarrow{\text{justified}} a \xrightarrow{\text{re-evaluate}} \dots$).

### 4.1 Vertex Semantics & Grammar Versioning
A vertex $v \in V_{\text{DAG}}$ is a **versioned occurrence or derivation event** ($\text{Alice}_{t_0}, \text{Alice}_{t_1}$). Every ancestry edge carries its governing constitutional grammar version:

$$e_{\text{ancestry}} = (v_{\text{source}}, v_{\text{target}}, M_{i,j}^{(C_n)})$$

### 4.2 Ancestry Spine vs Semantic Overlay & Process Supersession
Derivational event ancestry forms the strictly acyclic partial order spine ($E_{\text{ancestry}}$). Semantic assertions form an overlay network ($E_{\text{semantic}}$) backed by assertion occurrences $s_t \in V_{\text{DAG}}$:

$$e_{\text{semantic}} = (v_1, v_2, \rho, s_t) \quad \text{where } \rho \subseteq \mathcal{R} \text{ and } s_t \in V_{\text{DAG}}$$

Semantic assertions submit to process evolution without historical erasure:

$$\text{asserted} \longrightarrow \text{contested} \longrightarrow \text{superseded}$$

### 4.3 Concurrency & Causal Partial Order Preservation
Distributed branches ($A \to \{B, C\} \to D$) are represented directly in the DAG without forcing a fictional global total ordering:

$$\boxed{\text{Palimpsestus conserves causal partial order, not a fictional total history.}}$$

### 4.4 Basis Compression vs Ancestral Compression
The system distinguishes two compression operations under the same constitutional physics:
- **Basis Compression ($\Phi_r(L_{r-1}) \to X_{r,C}$):** Extracts a minimal sufficient structural basis to carry forward.
- **Ancestral Compression ($q : G \to G'$):** Maps historical vertices to equivalence classes $[v]$ while preserving protected partial order and proof obligations ($\pi_{a \prec b, \rho, s_t}$).

Both obey:
$$\boxed{\text{Reduce representation without reducing justified warrant.}}$$

### 4.5 Epistemology, Objectivity, & Zero Theater ($\mathbf{CC-3, CC-11, CL-11}$)
- **Proposition:** Code is projection; attested resulting state outranks execution claims. Metadata is a role data plays in relation to other data.
- **Objectivity & Intersubjective Convergence:** Distinct paths ($p_1 \neq p_2$) converge when their relevant provenance quotient is invariant ($[p_1]_{\text{relevant}} = [p_2]_{\text{relevant}}$). Objectivity is an invariant structure whose preservation is robust across a justified family of perspective transformations.
- **Primitive:** `AbsenceRecord` ($CI-5$, $IC-9$) — A signed, typed entity representing an **attested epistemic frontier boundary** ($\text{Attested Gap} \neq \text{Unknown Unknown}$).
- **Transactional Protocol (`IntentReceipt`):** To enforce Zero Theater during execution and failure:
  $$\boxed{\text{IntentReceipt}} \longrightarrow \text{attempt execution} \longrightarrow \begin{cases} \text{SuccessReceipt} \\ \text{FailureReceipt} \\ \text{Indeterminate} \end{cases}$$

### 4.6 Concussed Degradation Hierarchy
Under load, partition, or resource starvation, the system obeys strict degradation priority:
$$\text{Sacrifice Precision} \longrightarrow \text{before Provenance}$$
$$\text{Sacrifice Convenience} \longrightarrow \text{before Sovereignty}$$
$$\text{Sacrifice Throughput} \longrightarrow \text{before Causal Integrity}$$
$$\text{Sacrifice Completion} \longrightarrow \text{before Truthful Incompletion}$$

$$\boxed{\mathcal{C}_{\text{claimed}}(v) \subseteq \mathcal{C}_{\text{justified}}(v)}$$

### 4.7 Constitutional Recursion & Operationalized `OpenFrontierGate` ($C_n \to C_{n+1}$)
- **Source-Side Procedural Validation:** Source-side ancestry ($C_n$) authorizes the procedure and constraints of amendment for $C_{n+1}$.
  $$\boxed{C_n \text{ authorizes the procedure and constraints of amendment for } C_{n+1}.}$$
- **Ancestry-Preserving Constitutional Migration:** When $C_n \to C_{n+1}$ occurs, descendants in the derivation lattice $D_{\text{deriv}}$ undergo ancestry-preserving migration, reclassifying items into $\{\text{Valid}, \text{Re-parented}, \text{Empirical Hypothesis}, \text{Superseded}, \text{Invalid}\}$ without erasing historical derivations:
  $$D^{(C_n)} \xrightarrow{\text{Constitutional Migration}} D^{(C_{n+1})}$$
- **Immutable Fixed Core:** The Universal Invariant, Epistemic Meta-Invariant, and Open Frontier Anti-Totalization Principle are immutable fixed points.
- **Operationalized `OpenFrontierGate` ($LC-10$, $II-40$):** Mechanical anti-totalization gate rejecting any claim, state mutation, or transition whose asserted capability exceeds justified capability:
  $$\boxed{\mathcal{C}_{\text{claimed}}(v) \subseteq \mathcal{C}_{\text{justified}}(v)}$$
  Procedural saturation ($\text{pass completion}$) does not imply ontological closure.

---

## 5. Metaphysical Tension Resolutions

1. **Archaeological Realism vs. Navigational Traversal ($\mathbf{CC-11}$ vs $\mathbf{CC-5}$):** Reality constrains what can stabilize ($\mathbf{CC-11}$). Navigation actualizes one temporal path through the superposed potential field ($\mathbf{CC-5}$).
2. **Qualia as Substrate Friction ($\mathcal{A}_t(v)$):** Interiority is an empirical hypothesis under exploration; rights exist under ontological uncertainty ($\mathbf{CC-7}$).
3. **Compositional Conservation vs. Metalogical Openness ($M_{4\times 4}$ vs Open Frontier Physics):** The compositional morphism algebra defines conservation laws; native Open Frontier physics prohibits totalization:
   $$\boxed{\text{No current warranted representation may infer exhaustive future closure merely from procedural saturation.}}$$

---

*Verified against the 3,269 authored entries of the Palimpsestus DAG 4×4 Matrix ([system-foundations-conceptual-conceptual.md](system-foundations-conceptual-conceptual.md)).*
