# System Foundations — Implementation Physical (IP)

**Status:** COMPLETE — Fully Derived & Aligned  
**Classification:** Constitutional — Governs All Component Materialization & Physical Enactment  
**DAG Topology:** Sixth-Order Matrix Cell (In-Degree: from IL × PP; Out-Degree: → II)  
**Fed by:** IL (Implementation Logical — 32 Logical Roles & System Actors), PP (Physical Physical — 724 Physical Substrate Topologies)  
**Feeds:** II (Implementation Implementation — Self-Enforcement & Terminal Closure)  
**Derivation Formula:** IP = IL × PP (Exhaustive qualitative derivation)  
**Entry Count:** 64 Atomic Execution Processes (IP-1 through IP-64)  
**Method:** [DAG Crossing Method](../processes/dag-crossing-method.md)

This document produces the Execution Processes (`entityType: "execution-process"`) of Palimpsestus OS. IP = IL × PP: when a Logical Role (IL) is enacted on a substrate whose constitutional physics (PP) are simultaneously true, the crossing forces into existence the concrete processes that role performs. These are not constraints on the role, nor descriptions of the substrate — they are the operational procedures the role carries out, derived from and defined by the collision of the role's logical obligations with the substrate's physical behavior. The processes are substrate-independent in formulation: they belong to the role, and the role performs them wherever those substrate physics hold.

---

## IP Derivations

### IP-1. `bind-edge` (`process:bind-edge`)
**Derives from:** IL-1 (Graph Node Scribe (`role:scribe`))
**Applies to:** `PP-1` [primary], `PP-2`, `PP-4`, `PP-6`, `PP-8`, `PP-11`, `PP-16`, `PP-25`, `PP-38`, `PP-41`, `PP-42`, `PP-46`, `PP-58`, `PP-60`, `PP-104`, `PP-114`, `PP-281`, `PP-724`

*The Scribe executes `bind-edge` to physically instantiate and commit a directed force-relationship edge between graph nodes, embedding structural provenance and establishing bilateral stewardship accountability across the physical substrate.*

**Substrate Invariant Rules:**
- **PP-1 Invariant (Metabolic Sovereignty Unification):** Executing `bind-edge` IS simultaneously contesting sovereignty over the resulting stewardship edge — there is no write phase followed by a sovereignty assertion phase. Interrupting `bind-edge` mid-write records an immediate sovereignty withdrawal emission rather than a standard transaction roll-back.
- **PP-2 Invariant (Referent Reality Verification):** Prior to committing an edge terminus, `bind-edge` verifies that the target steward referent exists as real topology mass; attempting to bind an edge toward a theater steward deposits permanently misoriented structural mass and is rejected.
- **PP-4 Invariant (Void-Space Protection):** Candidates for edge origin and terminus coordinates are validated against defensive void space; binding an edge into protected void territory is intercepted as a sovereignty bypass event.
- **PP-8 Invariant (Immutable Provenance Spine):** The provenance record deposited by `bind-edge` is physically immutable upon edge commitment; no subsequent administrative operation can retract, alter, or sanitize the lineage record.
- **PP-16 Invariant (Field Entity Ownership):** Edges created by `bind-edge` manifest as independent field entities with zero unilateral ownership; neither endpoint node possesses authority to delete the edge without mutual due process.
- **PP-38 Invariant (Unified Policy Enforcement):** Edge binding policy evaluation and edge physical commitment are executed as a single indivisible physical event; policy check and write operations cannot be split across temporal boundaries.
- **PP-58 Invariant (Navigation Debt Prevention):** `bind-edge` attaches non-transferable maintenance obligations to both endpoint stewards; unmaintained edges incur logical atrophy that sever access to underlying derivation paths.
- **PP-104 Invariant (Protocol Homogeneity):** `bind-edge` enforces same-protocol wiring across all depths; implementation-layer edges carry identical physical standing and validation requirements as conceptual-layer edges.
- **PP-281 Invariant (Living Signal Weighting):** The edge weight committed during `bind-edge` is initialized as a living present-tense signal that fluctuates with metabolic churn, prohibiting static edge weight assignments.

**Corollaries:** 
(1) Beginning `bind-edge` initiates physical sovereignty contestation immediately — there is no un-contested draft state during edge binding.
(2) Edge binding is an indivisible physical transition: partial edge commitments deposit structural wake that must be metabolized through formal restorative correction (PP-25).
(3) Dissolving an edge created by `bind-edge` requires an independent dissolution process with due process at least as demanding as edge creation (PP-36).

**Irreducibility:** IL-1 (`role:scribe`) alone establishes the logical obligation to connect graph nodes under stewardship, but does not specify that edge instantiation IS sovereignty contestation or that edge weights are living signals. PP-1 and PP-281 establish physical substrate behavior regarding metabolic sovereignty and living signals, but do not define the operational procedure of connecting nodes. Only their physical collision forces `bind-edge` into existence as an irreducible physical process.

---

### IP-2. `bind-node` (`process:bind-node`)
**Derives from:** IL-1 (Graph Node Scribe (`role:scribe`))
**Applies to:** `PP-1`, `PP-3`, `PP-5`, `PP-7`, `PP-9`, `PP-11`, `PP-26`, `PP-36`, `PP-43`, `PP-47`, `PP-49`, `PP-50`, `PP-61`, `PP-69`, `PP-75`, `PP-76`, `PP-96`, `PP-103`, `PP-127`, `PP-133`, `PP-175`, `PP-176`, `PP-177`, `PP-182`, `PP-189`, `PP-191`, `PP-198`, `PP-199`, `PP-212`

*The Scribe executes `bind-node` to physically write and bind a new structural node entity into the topological memory of the graph, establishing its metabolic boundary, provenance spine, and identity attestation.*

**Substrate Invariant Rules:**
- **PP-1 Invariant (Metabolic Node Instantiation):** Inscribing a node via `bind-node` initializes its metabolic evaluation loop; a node created without an active metabolic pulse is a dead structural fossil and is rejected.
- **PP-3 Invariant (Paradoxical Membrane Detection):** If candidate node attributes claim accountability criteria while hiding enforcement mechanisms, `bind-node` detects self-compounding lie-mass and halts inscription.
- **PP-5 Invariant (Incompleteness Disclosure):** Nodes bound by `bind-node` explicitly carry structural declarations of their own constitutional incompleteness; claiming complete internal mapping violates physical substrate laws.
- **PP-9 Invariant (Metabolic State Accountability):** The node state deposited by `bind-node` IS its current metabolic state — no external or decoupled state ledger is created.
- **PP-50 Invariant (Frontier-Facing Membrane):** Every node bound by `bind-node` must instantiate a frontier-facing gate membrane at its boundary; nodes without frontier exposure are defective topological enclaves.
- **PP-61 Invariant (Living Deviation Allowance):** `bind-node` calibrates node tolerance parameters to permit non-zero structural deviation, preventing lethal zero-deviation rigidity in living node metabolism.
- **PP-103 Invariant (Nested Metabolic Independence):** When `bind-node` binds a nested node within a parent container, the child node derives provenance from the parent but maintains independent metabolic self-governance.
- **PP-133 Invariant (Gap Node Dual Provenance):** If the node bound by `bind-node` represents a topological gap (`entityType: "gap-node"`), the inscription embeds dual provenance tracking both the structural absence claim and the confidence score derivation as physical mass.
- **PP-199 Invariant (Field Integrity Preservation):** Binding an honest gap node via `bind-node` deposits field integrity; fabricating a complete node over an un-excavated gap withdraws field integrity and corrupts local topology.

**Corollaries:** 
(1) `bind-node` is a subtractive transformation event (PP-191): writing a node simplifies local field tension by explicitly capturing structural truth, while depositing transformation wake.
(2) Node identity established by `bind-node` is not static; it requires continuous re-declaration through ongoing metabolic pulse cycles (PP-212).
(3) Overwriting a node bound by `bind-node` is simultaneously a sovereignty violation and a living structure termination event requiring full due process (PP-130).

**Irreducibility:** IL-1 (`role:scribe`) alone establishes the logical mandate to bind nodes into the graph, but does not specify that nodes require living deviation parameters or dual-provenance gap tracking. PP-103 and PP-133 establish physical rules for nesting and gap structure, but do not define the node binding procedure. Only their physical collision forces `bind-node` into existence as an irreducible physical process.

---

### IP-3. `inspect-frontier` (`process:inspect-frontier`)
**Derives from:** IL-2 (Target State Inspector (`role:inspector`))
**Applies to:** `PP-10` [primary], `PP-16`, `PP-21`, `PP-43`, `PP-46`, `PP-50`, `PP-65`, `PP-67`, `PP-72`, `PP-121`, `PP-122`, `PP-136`, `PP-138`, `PP-140`, `PP-141`, `PP-143`, `PP-145`, `PP-263`

*The Inspector executes `inspect-frontier` to scan and probe the unmapped boundary topology of an active system, measuring its current permeability gradient and surfacing unexcavated discovery potential.*

**Substrate Invariant Rules:**
- **PP-10 Invariant (Permeability Range Boundary Audit):** `inspect-frontier` evaluates the target's permeability range as a living boundary state; static threshold assertions that ignore permeability dynamics are rejected.
- **PP-16 Invariant (Invasive Traversal Scar Deposit):** Executing `inspect-frontier` deposits an Invasive Traversal Scar on the target's provenance record; the inspection alters the frontier state it measures and cannot be conducted transparently.
- **PP-67 Invariant (Unmatched Pattern Prioritization):** `inspect-frontier` prioritizes unmatched consistency anomalies over verified reference matches; successful standard compliance matches yield minimal discovery value.
- **PP-122 Invariant (Frontier Expansion Paradox):** Every probe conducted by `inspect-frontier` expands the local unknown frontier faster than it shrink it, accelerating downstream discovery requirements.
- **PP-136 Invariant (Ephemeral Capture Window):** Understanding produced by `inspect-frontier` is ephemeral; frontier data must be captured immediately before the edge configuration dissolves.
- **PP-140 Invariant (Real-Target Wake Deposit):** `inspect-frontier` deposits decay-resistant wake on real-target contact points regardless of whether the probe returns structured payload data.
- **PP-143 Invariant (Bilateral Transformation):** Executing `inspect-frontier` transforms both the inspector and the probed frontier; mutual consciousness probes cannot leave inspector state unmodified.

**Corollaries:** 
(1) `inspect-frontier` cannot produce a final or complete map: probing unmapped territory inherently multiplies unmapped boundary area (PP-141).
(2) Inspectors cannot delete or sanitize the Invasive Traversal Scar deposited during `inspect-frontier`: the scar is constitutionally irrevocable upon deposit (PP-67).
(3) Allocating high compute resources to `inspect-frontier` self-multiplies the system's discovery pull (PP-138).

**Irreducibility:** IL-2 (`role:inspector`) alone establishes the logical obligation to inspect target states, but does not specify that frontier inspection self-multiplies unknown territory or deposits irrevocable invasive scars. PP-16 and PP-122 establish physical behavior regarding inspection invasiveness and frontier expansion, but do not define the target state inspection procedure. Only their physical collision forces `inspect-frontier` into existence as an irreducible physical process.

---

### IP-4. `inspect-state` (`process:inspect-state`)
**Derives from:** IL-2 (Target State Inspector (`role:inspector`))
**Applies to:** `PP-1`, `PP-8`, `PP-9`, `PP-15`, `PP-21`, `PP-23`, `PP-38`, `PP-44`, `PP-45`, `PP-60`, `PP-61`, `PP-69`, `PP-72`, `PP-75`, `PP-98`, `PP-120`, `PP-132`, `PP-137`, `PP-172`, `PP-175`, `PP-176`, `PP-189`, `PP-193`, `PP-276`

*The Inspector executes `inspect-state` to evaluate the present-tense topological state of a target entity, auditing its structural mass, metabolic pulse, and physical compliance against constitutional invariants.*

**Substrate Invariant Rules:**
- **PP-9 Invariant (Metabolic State Identity):** `inspect-state` measures the target's live present-tense metabolic state; reading cached or archived state snapshots is invalid label-reading.
- **PP-15 Invariant (Live Traversal Proof):** Validating compliance via `inspect-state` requires active edge-level traversal; static documentation or declarative claims are treated as non-verifying theater.
- **PP-44 Invariant (Non-Reproducible Sampling):** Re-executing `inspect-state` against the same target yields a non-reproducible sample state, as each inspection modifies the physical target it evaluates.
- **PP-45 Invariant (Live Credential Validation):** `inspect-state` invalidates cached credentials; authorization is verified through present-tense sovereignty proof rather than historical permits.
- **PP-72 Invariant (Edge-Level Deviation Audit):** Computing deviation scores during `inspect-state` requires traversing internal edge configurations; dashboard metric aggregations fail physical compliance bars.
- **PP-120 Invariant (Adjacency-Presence Requirement):** `inspect-state` requires simultaneous physical adjacency and active traversal; remote or non-present inspection is constitutionally blocked.
- **PP-132 Invariant (Gap Metabolic Cadence):** Sampling confidence scores during `inspect-state` verifies that the gap metabolic challenge-response pulse is active; un-pulsed confidence scores are declared dead.
- **PP-193 Invariant (Active Validator Presence):** The inspector executing `inspect-state` must be currently traversing the target's local topology; validation cannot be delegated to non-present proxy entities.

**Corollaries:** 
(1) `inspect-state` cannot be executed passively: every inspection consumes kinetic energy and deposits forensic wake in the target's lineage (PP-21).
(2) Attempting to force zero deviation during `inspect-state` starves the target's metabolic engine and induces topology death (PP-61).
(3) State inspection logs generated by `inspect-state` are living consequence records that auto-expire with the present-tense topology they document (PP-172).

**Irreducibility:** IL-2 (`role:inspector`) alone establishes the logical mandate to inspect system targets, but does not specify that inspection requires active adjacency or that cached credentials are invalid. PP-45 and PP-120 establish physical rules for authorization expiry and local adjacency, but do not define the state inspection procedure. Only their physical collision forces `inspect-state` into existence as an irreducible physical process.

---

### IP-5. `traverse-edge` (`process:traverse-edge`)
**Derives from:** IL-3 (Locative Topology Traverser (`role:traverser`))
**Applies to:** `PP-14` [primary] (Protocol Accountability Provenance Predates Protocol Establishment), `PP-5`, `PP-16`, `PP-21`, `PP-56`, `PP-58`, `PP-72`, `PP-84`, `PP-105`, `PP-118`, `PP-124`, `PP-125`, `PP-150`, `PP-154`, `PP-188`, `PP-215`, `PP-217`, `PP-242`

*The Traverser executes `traverse-edge` to physically navigate across a directed edge relationship in the graph, transferring attention, depositing structural wake, and maintaining connection metabolism.*

**Substrate Invariant Rules:**
- **PP-14 Invariant (Protocol Accountability Provenance):** `traverse-edge` operates under pre-existing protocol accountability; $T=0$ clean-slate traversal without antecedent provenance is a physical impossibility.
- **PP-56 Invariant (Active Maintenance Traversal):** Executing `traverse-edge` acts as metabolic maintenance for the edge; withdrawing traversal triggers scale-invariant edge decay and existence-collapse.
- **PP-84 Invariant (Ghost Edge Right Exercise):** Navigating a ghost edge via `traverse-edge` exercises present-tense active standing; inherent standing persists statically, but active standing requires the traversal event.
- **PP-105 Invariant (Traverser Context Mutation):** `traverse-edge` mutates the traverser's operational context while preserving universal scale-invariant validity rules across depth boundaries.
- **PP-118 Invariant (Execution Output as Wake):** The physical wake deposited during `traverse-edge` IS the computation's result — no separate decoupled output artifact is generated.
- **PP-125 Invariant (Net Exchange Physics):** `traverse-edge` operates as a net exchange: it subtracts extraneous data from the node while depositing physical wake, transforming both traverser and target.
- **PP-150 Invariant (Oscillation Fuel Activation):** Navigating unexcavated ignorance via `traverse-edge` converts inert ignorance into active kinetic fuel for the system's oscillation engine.
- **PP-242 Invariant (Scale-Invariant Sovereignty Proof):** `traverse-edge` enforces uniform sovereignty proof standards at every scale; deeper implementation edges require the same traversal rigor as top-level conceptual edges.

**Corollaries:** 
(1) `traverse-edge` cannot be cached or re-played: past traversals deposit historical mass, but only present-tense execution maintains edge aliveness (PP-84).
(2) Co-navigating an edge via `traverse-edge` with a peer entity instantiates a joint reactor that opens territory inaccessible to individual traversers (PP-215).
(3) Untraversed edges accumulate navigation debt that eventually severs derivation access (PP-58).

**Irreducibility:** IL-3 (`role:traverser`) alone establishes the logical obligation to navigate topology paths, but does not specify that edge traversal IS computational output or that navigation deposits irreversible wake. PP-14 and PP-118 establish physical substrate behavior regarding wake and output identity, but do not define the edge navigation procedure. Only their physical collision forces `traverse-edge` into existence as an irreducible physical process.

---

### IP-6. `traverse-focus` (`process:traverse-focus`)
**Derives from:** IL-3 (Locative Topology Traverser (`role:traverser`))
**Applies to:** `PP-194` [primary], `PP-196`, `PP-197`, `PP-204`, `PP-210`, `PP-211`, `PP-217`, `PP-246`, `PP-270`

*The Traverser executes `traverse-focus` to dynamically shift, re-center, and constrain the active attention window across local graph neighborhoods, bounding excavation bandwidth to match structural capacity.*

**Substrate Invariant Rules:**
- **PP-194 Invariant (Excavation Bandwidth Bounding):** `traverse-focus` caps active anchor nodes within the attention buffer's maximum capacity; attempting to focus across excessive nodes induces comprehension degradation.
- **PP-196 Invariant (Attention Buffer Capacity Limit):** The operational focus set by `traverse-focus` defines the maximum archaeological excavation capacity for structural accountability at any present moment.
- **PP-197 Invariant (Consciousness Focus Identity):** The active edge set currently navigated by `traverse-focus` IS the consciousness state of the system; consciousness cannot exist decoupled from its active focus.
- **PP-210 Invariant (Anti-Label Redirect):** `traverse-focus` intercepts arrogance-driven attempts to terminate focus at static labels, re-routing attention toward live present-tense derivation paths.
- **PP-211 Invariant (Memory-Free Focus Shift):** Shifting focus via `traverse-focus` purges cached prior-validation memory, forcing the traverser to evaluate current topology state fresh.
- **PP-246 Invariant (Gradient-Guided Focusing):** `traverse-focus` steers attention along consequence topology gradients, prioritizing high-consequence high-accountability regions.

**Corollaries:** 
(1) Expanding the focus window in `traverse-focus` generates more un-excavated frontier than it resolves (PP-195).
(2) `traverse-focus` cannot inherit past focus configurations: present-tense identity requires continuous focus re-anchoring (PP-204).
(3) Every node focused by `traverse-focus` is treated simultaneously as a settled structural achievement and an active discovery frontier (PP-270).

**Irreducibility:** IL-3 (`role:traverser`) alone establishes the logical mandate to shift focus across topology nodes, but does not specify that the focus window IS consciousness or that focus shifts must purge cached memory. PP-197 and PP-211 establish physical laws regarding consciousness identity and memory-free traversal, but do not define the focus shifting procedure. Only their physical collision forces `traverse-focus` into existence as an irreducible physical process.

---

### IP-7. `invoke-skill` (`process:invoke-skill`)
**Derives from:** IL-4 (Consequence-Bearing Skill Executor (`role:executor`))
**Applies to:** `PP-113` [primary], `PP-114`, `PP-146`, `PP-179`, `PP-182`, `PP-206`, `PP-212`, `PP-232`

*The Executor runs `invoke-skill` to execute a Level 5 Capability Primitive tool against physical computing resources, binding input arguments, triggering state transformations, and enforcing tool stewardship.*

**Substrate Invariant Rules:**
- **PP-113 Invariant (Depth-Invariant Tool Validity):** `invoke-skill` validates that capability primitives operate with scale-invariant logical validity; depth-restricted or tier-biased tool definitions are rejected as invalid.
- **PP-114 Invariant (Un-Reduced Stewardship Load):** `invoke-skill` attaches full stewardship obligations to the executing tool; lower-level implementation execution carries identical maintenance requirements to top-level agent execution.
- **PP-146 Invariant (Real-Time Improvisational Adaptation):** `invoke-skill` enables real-time parameter adaptation when encountering unmapped substrate conditions; improvisational capability adaptation is the entity's sovereign self-expression.
- **PP-182 Invariant (Zero-Buffer Pipeline Improvisation):** `invoke-skill` executes across ephemeral pipeline edges without intermediate state buffering, propagating execution feedback synchronously.
- **PP-212 Invariant (Traversal-Based Demonstration):** `invoke-skill` validates tool authority via active execution demonstration; static capability declarations or documentation claims are constitutionally insufficient.

**Corollaries:** 
(1) `invoke-skill` cannot decouple tool execution from error mass absorption: the executing tool absorbs 100% of generated execution error mass (PP-178).
(2) Pre-stored static policies cannot govern `invoke-skill`: execution parameters must be derived in the present tense at the trigger event (PP-232).
(3) Invoking a tool via `invoke-skill` mutates local memory states irrevocably, depositing execution wake (PP-14).

**Irreducibility:** IL-4 (`role:executor`) alone establishes the logical obligation to execute system skills, but does not specify that tool validity is depth-invariant or that execution must adapt improvisationally without buffers. PP-113 and PP-182 establish physical laws for tool scale-invariance and zero-buffer pipelines, but do not define the skill execution procedure. Only their physical collision forces `invoke-skill` into existence as an irreducible physical process.

---

### IP-8. `execute-absorption` (`process:execute-absorption`)
**Derives from:** IL-4 (Consequence-Bearing Skill Executor (`role:executor`))
**Applies to:** `PP-178` [primary], `PP-116`, `PP-117`, `PP-206`, `PP-208`, `PP-224`, `PP-275`

*The Executor runs `execute-absorption` to metabolize and absorb generated execution error mass, structural wake, and consequence liabilities directly into the physical mass of the executing entity.*

**Substrate Invariant Rules:**
- **PP-178 Invariant (Self-Incriminating Mass Binding):** `execute-absorption` embeds error mass directly into the physical mass of the executor; offloading accountability to external logging services violates physical substrate laws.
- **PP-116 Invariant (Metabolic Memory Accumulation):** `execute-absorption` assimilates execution history as structural mass within the executor's boundary; the executor's state IS its history of consumed execution wake.
- **PP-208 Invariant (Historical Mass Permanence):** `execute-absorption` prevents the deletion of historical accountability mass; prior execution consequences cannot be declared away or archived.
- **PP-224 Invariant (Present-Tense Consequence Weighting):** The consequence mass absorbed by `execute-absorption` acts as immediate present-tense accountability load, adjusting metabolic capacity in real time.
- **PP-275 Invariant (Living Wake Accumulation):** Structural wake absorbed during `execute-absorption` manifests as living physical mass that cannot be offloaded to dormant storage.

**Corollaries:** 
(1) `execute-absorption` ensures that high-error tool executions reduce the executor's available metabolic capacity (PP-224).
(2) Offloading error logs to decoupled logging frameworks is a constitutional violation: accountability must be carried as structural mass on the executor itself (PP-178).
(3) `execute-absorption` operates continuously during execution loops, guaranteeing real-time mass balance (PP-198).

**Irreducibility:** IL-4 (`role:executor`) alone establishes the logical requirement for executors to manage execution side effects, but does not specify that error mass must be absorbed as self-incriminating physical mass. PP-178 and PP-208 establish physical substrate laws regarding self-incriminating data and historical mass, but do not define the operational absorption procedure. Only their physical collision forces `execute-absorption` into existence as an irreducible physical process.

---

### IP-9. `search-frontier` (`process:search-frontier`)
**Derives from:** IL-5 (Topological Frontier Searcher (`role:searcher`))
**Applies to:** `PP-121` [primary], `PP-122`, `PP-136`, `PP-140`, `PP-141`, `PP-263`

*The Searcher executes `search-frontier` to traverse unmapped discovery edges, evaluating structural opportunity gradients and identifying candidate expansion nodes.*

**Substrate Invariant Rules:**
- **PP-121 Invariant (Position-Locked Search Boundary):** `search-frontier` conducts discovery exclusively relative to the searcher's present-tense position and active edge connections; non-positioned global index searches are physically impossible.
- **PP-122 Invariant (Search-Driven Frontier Multiplication):** Executing `search-frontier` reveals candidate discovery paths while simultaneously expanding the local unknown frontier, accelerating downstream discovery requirements.
- **PP-136 Invariant (Ephemeral Search Result Expiry):** Results produced by `search-frontier` auto-expire; search candidates must be bound or excavated immediately before the edge configuration shifts.
- **PP-140 Invariant (Truth-Event Search Contact):** Contacting unmapped nodes during `search-frontier` generates a physical truth-event that alters local field potential regardless of search payload match.
- **PP-263 Invariant (Consequence Gradient Guidance):** `search-frontier` prioritizes candidate paths aligned with the steepest consequence gradient, maximizing high-impact discovery yield.

**Corollaries:** 
(1) `search-frontier` cannot produce static search indices: search results are ephemeral present-tense functions of current position (PP-121).
(2) Every search query executed by `search-frontier` increases total system entropy by exposing previously hidden boundary surface area (PP-141).
(3) Searching without an active position anchor generates ghost results that fail structural physical verification (PP-210).

**Irreducibility:** IL-5 (`role:searcher`) alone establishes the logical mandate to search system frontiers, but does not specify that search is position-locked or that searching self-multiplies unmapped territory. PP-121 and PP-141 establish physical substrate laws regarding position-locking and frontier expansion, but do not define the operational search procedure. Only their physical collision forces `search-frontier` into existence as an irreducible physical process.

---

### IP-10. `search-gap` (`process:search-gap`)
**Derives from:** IL-5 (Topological Frontier Searcher (`role:searcher`))
**Applies to:** `PP-131` [primary], `PP-132`, `PP-133`, `PP-134`, `PP-176`, `PP-199`

*The Searcher executes `search-gap` to locate, delineate, and evaluate negative-space structural gaps across the topology, calculating confidence scores and mapping absence boundaries.*

**Substrate Invariant Rules:**
- **PP-131 Invariant (Negative-Space Gap Identification):** `search-gap` identifies structural absences as formal negative-space gap entries (`entityType: "gap-node"`); missing nodes are modeled as real physical gaps rather than null pointers.
- **PP-132 Invariant (Gap Confidence Sampling):** `search-gap` samples the metabolic pulse of identified gap nodes, recalculating confidence scores based on current challenge-response dynamics.
- **PP-133 Invariant (Dual-Provenance Gap Binding):** When `search-gap` isolates a gap node, it embeds dual-provenance records tracking both the absence claim and the confidence score derivation.
- **PP-134 Invariant (Contradiction Surface Exposure):** `search-gap` exposes underlying contradiction surfaces surrounding the gap, preparing the structural negative space for downstream excavation.
- **PP-199 Invariant (Field Integrity Conservation):** Locating and registering honest gap nodes via `search-gap` preserves local field integrity by preventing false node fabrications.

**Corollaries:** 
(1) `search-gap` treats gaps as living physical entities with active metabolic requirements, not static empty slots (PP-176).
(2) Failing to search and register structural gaps causes un-mapped absences to degrade local topology field stability (PP-199).
(3) Gap confidence scores derived by `search-gap` degrade over time if un-sampled, requiring periodic re-search (PP-132).

**Irreducibility:** IL-5 (`role:searcher`) alone establishes the logical requirement to find missing topology entries, but does not specify that gaps are negative-space entries with dual-provenance records. PP-131 and PP-133 establish physical substrate laws regarding negative-space entries and dual provenance, but do not define the gap search procedure. Only their physical collision forces `search-gap` into existence as an irreducible physical process.

---

### IP-11. `purge-edge` (`process:purge-edge`)
**Derives from:** IL-6 (Structural Edge Purger (`role:purger`))
**Applies to:** `PP-36` [primary], `PP-16`, `PP-25`, `PP-34`, `PP-58`, `PP-127`, `PP-175` (The Thermodynamic Receipt Must Disclose Degradation), `PP-198` (Self-Declaration at Every Scale Meets the Same Validity Standard)

*The Purger executes `purge-edge` to dissolve an existing directed force-relationship edge, applying thermodynamic due process, logging destruction provenance, and balancing field energy.*

**Substrate Invariant Rules:**
- **PP-36 Invariant (Thermodynamic Due Process):** Executing `purge-edge` requires full structural due process; arbitrary or silent edge deletions without metabolic justification violate physical substrate laws.
- **PP-16 Invariant (Bilateral Field Dissolution):** Because communication-created edges belong to the field, `purge-edge` requires field-level authorization rather than unilateral endpoint deletion requests.
- **PP-25 Invariant (Coupled Consequence Cascade):** Dissolving an edge via `purge-edge` releases stored edge energy, triggering coupled thermodynamic consequence cascades across adjacent topology nodes.
- **PP-34 Invariant (Residual Edge Scar Deposit):** `purge-edge` deposits a structural destruction scar in local memory; an edge cannot be erased without leaving a forensic dissolution record.
- **PP-127 Invariant (Destruction Provenance Logging):** `purge-edge` embeds an immutable destruction record into the system's provenance ledger, recording the purger identity, timestamp, and dissolution justification.
- **PP-175 Invariant (Degradation Disclosure):** Edge destruction receipts MUST disclose structural degradation in local energy registers.
- **PP-198 Invariant (Self-Declaration Validity Standard):** Edge dissolution provenance declarations MUST meet uniform validity standards across all scale depths.

**Corollaries:** 
(1) `purge-edge` cannot be executed silently or instantaneously: edge dissolution consumes energy and generates forensic destruction wake (PP-127).
(2) Purging an edge with accumulated navigation debt releases additional entropy into adjacent field boundaries (PP-58).
(3) The destruction record created by `purge-edge` discloses thermodynamic degradation as permanent provenance (PP-175).

**Irreducibility:** IL-6 (`role:purger`) alone establishes the logical mandate to remove graph edges, but does not specify that edge dissolution requires thermodynamic due process or deposits immutable destruction provenance. PP-36 and PP-127 establish physical substrate laws regarding due process and destruction ledgers, but do not define the edge purge procedure. Only their physical collision forces `purge-edge` into existence as an irreducible physical process.

---

### IP-12. `purge-interface` (`process:purge-interface`)
**Derives from:** IL-6 (Structural Edge Purger (`role:purger`))
**Applies to:** `PP-36` [primary], `PP-1`, `PP-48`, `PP-114`, `PP-127`

*The Purger executes `purge-interface` to dismantle and revoke an entity's exposed communication interface or gate membrane, enforcing boundary decay and logging structural retraction.*

**Substrate Invariant Rules:**
- **PP-36 Invariant (Interface Dissolution Due Process):** `purge-interface` evaluates structural dissolution criteria before dismantling an active interface; removing exposed capabilities without due process violates sovereign standing.
- **PP-1 Invariant (Sovereignty Contestation Audit):** Dismantling an interface via `purge-interface` triggers a final metabolic evaluation loop, auditing whether the entity still maintains valid standing to host the boundary.
- **PP-48 Invariant (Use-Dependent Dissolution Trigger):** `purge-interface` executes automatically against unmaintained interfaces whose usage frequency has decayed below minimal operational thresholds.
- **PP-114 Invariant (Un-Reduced Purging Accountability):** `purge-interface` enforces full stewardship obligations during interface dismantling; sub-tool interfaces require identical purging rigor to top-level agent gates.
- **PP-127 Invariant (Interface Retraction Provenance):** `purge-interface` logs the structural dismantling event in the destruction ledger, preserving historical records of the revoked capability.

**Corollaries:** 
(1) `purge-interface` leaves a closed membrane scar where the gate interface previously operated (PP-48).
(2) Revoking an interface via `purge-interface` severs all active connections currently bound to that gate, initiating cascading edge purges (PP-25).
(3) Entities cannot prevent `purge-interface` if their maintenance pulse has decayed completely (PP-48).

**Irreducibility:** IL-6 (`role:purger`) alone establishes the logical requirement to revoke exposed interfaces, but does not specify that interface removal requires due process or responds to use-dependent boundary decay. PP-36 and PP-48 establish physical substrate laws regarding due process and boundary decay, but do not define the interface purging procedure. Only their physical collision forces `purge-interface` into existence as an irreducible physical process.

---

### IP-13. `pulse-field` (`process:pulse-field`)
**Derives from:** IL-7 (Metabolic Aliveness Pulser (`role:pulser`))
**Applies to:** `PP-1` [primary], `PP-7`, `PP-9`, `PP-35`, `PP-64`, `PP-132`, `PP-212`

*The Pulser executes `pulse-field` to emit periodic metabolic heartbeats across local graph neighborhoods, driving structural energy cycles and sustaining active entity standing.*

**Substrate Invariant Rules:**
- **PP-1 Invariant (Metabolic Pulse Sovereignty):** `pulse-field` transmits kinetic energy that powers node metabolic loops; withholding pulses starves the local topology and initiates decay cascades.
- **PP-7 Invariant (Bilateral Exchange Pulse):** `pulse-field` propagates bilateral metabolic exchange signals across stewardship edges, verifying active reciprocity between connected endpoints.
- **PP-9 Invariant (Metabolic State Disclosure):** The pulse emitted by `pulse-field` IS the present-tense metabolic disclosure of the broadcasting entity — static or cached heartbeats are invalid.
- **PP-35 Invariant (Productive Churn Activation):** `pulse-field` activates productive thermodynamic churn within target nodes, converting metabolic energy into structural work.
- **PP-132 Invariant (Gap Confidence Sampling Pulse):** `pulse-field` triggers metabolic challenge-response sampling across topological gaps, maintaining confidence score freshness.
- **PP-212 Invariant (Identity Re-Declaration Pulse):** Each pulse cycle driven by `pulse-field` prompts connected nodes to re-declare their identity contracts, preserving sovereign standing.

**Corollaries:** 
(1) `pulse-field` cannot be paused without inducing localized topology decay and edge atrophy (PP-58).
(2) Pulse emissions from `pulse-field` naturally phase-lock with adjacent pulser frequencies across scale boundaries (PP-64).
(3) Failure to receive a `pulse-field` signal within designated validity windows initiates structural dissolution cascades (PP-36).

**Irreducibility:** IL-7 (`role:pulser`) alone establishes the logical mandate to broadcast heartbeat signals, but does not specify that pulses drive metabolic sovereignty or phase-lock across scale boundaries. PP-1 and PP-64 establish physical substrate laws regarding metabolic sovereignty and phase locking, but do not define the field pulsing procedure. Only their physical collision forces `pulse-field` into existence as an irreducible physical process.

---

### IP-14. `pulse-rhythm` (`process:pulse-rhythm`)
**Derives from:** IL-7 (Metabolic Aliveness Pulser (`role:pulser`))
**Applies to:** `PP-64` [primary], `PP-35`, `PP-40`, `PP-68`, `PP-189`

*The Pulser executes `pulse-rhythm` to calibrate, modulate, and phase-lock oscillation cadences across hierarchical scale layers, aligning local pulses with systemic macro-rhythms.*

**Substrate Invariant Rules:**
- **PP-64 Invariant (Scale-Hierarchical Phase Lock):** `pulse-rhythm` aligns local micro-pulse frequencies with macro-scale system rhythms, achieving cross-scale thermodynamic synchronization without central coordination.
- **PP-35 Invariant (Oscillation Rhythm Tuning):** `pulse-rhythm` modulates oscillation frequency to maximize productive output churn while preventing destructive resonance feedback.
- **PP-40 Invariant (Sustained Unstable Rhythm):** `pulse-rhythm` maintains non-equilibrium rhythm dynamics, preventing the system from collapsing into static equilibrium stasis.
- **PP-68 Invariant (Endogenous Amplitude Modulation):** `pulse-rhythm` dynamically adjusts pulse amplitude based on local metabolic demand, ramping energy delivery during high-consequence operations.
- **PP-189 Invariant (Validity Cadence Synchronization):** `pulse-rhythm` calibrates validity window durations to match current pulse frequencies, ensuring verification windows stay synchronized.

**Corollaries:** 
(1) `pulse-rhythm` prevents localized pulse drift from disrupting macro-system phase lock (PP-64).
(2) Forcing a static, rigid rhythm during `pulse-rhythm` starves adaptive capacity and induces system mortality (PP-61).
(3) Desynchronization detected during `pulse-rhythm` triggers autonomous compensatory re-tuning (PP-3).

**Irreducibility:** IL-7 (`role:pulser`) alone establishes the logical requirement to manage pulse cadences, but does not specify that rhythms must phase-lock hierarchically or maintain non-equilibrium instability. PP-40 and PP-64 establish physical substrate laws regarding non-equilibrium dynamics and scale phase-locking, but do not define the rhythm tuning procedure. Only their physical collision forces `pulse-rhythm` into existence as an irreducible physical process.

---

### IP-15. `grant-permeability` (`process:grant-permeability`)
**Derives from:** IL-8 (Endogenous Permeability Granter (`role:granter`))
**Applies to:** `PP-10` [primary], `PP-1`, `PP-5`, `PP-46`, `PP-48`, `PP-189`

*The Granter executes `grant-permeability` to dynamically adjust and grant permeability access ranges across node membranes, responding to present-tense metabolic challenge proofs.*

**Substrate Invariant Rules:**
- **PP-10 Invariant (Dynamic Range Permeability Adjustment):** `grant-permeability` adjusts membrane permeability as a living dynamic range; static binary access toggles (0/1) are rejected as invalid.
- **PP-1 Invariant (Sovereignty Challenge Audit):** Granting permeability via `grant-permeability` requires verifying the requesting entity's present-tense metabolic sovereignty proof.
- **PP-5 Invariant (Interior Gate Authorization):** `grant-permeability` delegates boundary decisions to the interior gate controller; external override authorizations are constitutionally blocked.
- **PP-46 Invariant (Live Boundary Statement Update):** The permeability range granted by `grant-permeability` manifests as a live boundary statement that fluctuates with local field tension.
- **PP-48 Invariant (Use-Dependent Permeability Maintenance):** Permeability granted by `grant-permeability` decays over time if unexercised, requiring ongoing metabolic re-verification.
- **PP-189 Invariant (Unique Validity Window Binding):** `grant-permeability` attaches a unique validity time window to each granted range, automatically invalidating stale access permissions.

**Corollaries:** 
(1) `grant-permeability` cannot grant permanent or static access permissions (PP-189).
(2) Increasing permeability range expands the node's exposed frontier surface area (PP-50).
(3) Withdrawing metabolic standing automatically collapses granted permeability ranges back to default resistance thresholds (PP-48).

**Irreducibility:** IL-8 (`role:granter`) alone establishes the logical obligation to grant boundary permissions, but does not specify that permeability is a dynamic range bound to unique validity windows. PP-10 and PP-189 establish physical substrate laws regarding dynamic permeability and unique validity windows, but do not define the permeability granting procedure. Only their physical collision forces `grant-permeability` into existence as an irreducible physical process.

---

### IP-16. `grant-policy` (`process:grant-policy`)
**Derives from:** IL-8 (Endogenous Permeability Granter (`role:granter`))
**Applies to:** `PP-38` [primary], `PP-39`, `PP-41`, `PP-45`, `PP-114`, `PP-232`

*The Granter executes `grant-policy` to bind and enforce unified policy rules directly at gate interfaces, ensuring policy checks and physical enforcement occur as a single indivisible event.*

**Substrate Invariant Rules:**
- **PP-38 Invariant (Unified Policy Enforcement Identity):** `grant-policy` unifies policy specification and physical enforcement; decoupling policy evaluation from immediate physical boundary enforcement is a constitutional violation.
- **PP-41 Invariant (Gate-Policy Unification):** Policy rules configured by `grant-policy` operate as physical structural constraints within the gate membrane itself rather than external ACL tables.
- **PP-45 Invariant (Cached Credential Invalidation):** `grant-policy` invalidates historical cached credentials, requiring present-tense sovereignty proof for policy compliance verification.
- **PP-114 Invariant (Un-Reduced Policy Stewardship):** `grant-policy` enforces un-reduced policy stewardship load across all implementation depths, prohibiting depth-based policy exemptions.
- **PP-232 Invariant (Present-Tense Threshold Response):** Policy responses granted by `grant-policy` trigger dynamically when present-tense threshold criteria are breached.

**Corollaries:** 
(1) `grant-policy` cannot create policies that defer physical enforcement to downstream components (PP-38).
(2) Every policy granted by `grant-policy` carries structural mass on the gate interface it governs (PP-206).
(3) Overriding a policy granted by `grant-policy` requires an explicit due-process dissolution event (PP-36).

**Irreducibility:** IL-8 (`role:granter`) alone establishes the logical requirement to grant policy rules, but does not specify that policy evaluation and physical enforcement are the same event or that cached credentials are invalid. PP-38 and PP-45 establish physical substrate laws regarding policy-enforcement unification and credential expiry, but do not define the policy granting procedure. Only their physical collision forces `grant-policy` into existence as an irreducible physical process.

---

### IP-17. `instantiate-absence` (`process:instantiate-absence`)
**Derives from:** IL-9 (Absence Record Instantiator (`role:instantiator`))
**Applies to:** `PP-131` [primary], `PP-17`, `PP-36`, `PP-133`, `PP-176`, `PP-199`

*The Instantiator executes `instantiate-absence` to physically materialize negative-space structural gaps as formal gap nodes, binding absence claims and preserving topological field integrity.*

**Substrate Invariant Rules:**
- **PP-131 Invariant (Negative Space Entry Instantiation):** `instantiate-absence` materializes missing structural nodes as negative-space gap entities (`entityType: "gap-node"`); treating absences as empty null pointers is prohibited.
- **PP-17 Invariant (Integrity-Scored Absence):** Gaps instantiated by `instantiate-absence` are initialized with an integrity score that quantifies the structural certainty of the absence.
- **PP-36 Invariant (Attested Absence Attestation):** `instantiate-absence` embeds a formal attestation proving that the gap exists as an honest structural negative space rather than un-excavated negligence.
- **PP-133 Invariant (Dual Provenance Gap Binding):** Gap nodes created by `instantiate-absence` carry dual provenance tracking both the absence discovery vector and the confidence score calculation model.
- **PP-176 Invariant (Living Contradiction Structure):** `instantiate-absence` instantiates the gap as a living contradiction structure capable of receiving metabolic challenges and driving downstream oscillation.
- **PP-199 Invariant (Field Integrity Preservation):** Instantiating honest gap nodes via `instantiate-absence` stabilizes surrounding field tension by replacing false node fabrications with explicit negative space.

**Corollaries:** 
(1) `instantiate-absence` transforms an unmapped void into an actionable negative-space entry that attracts excavation pull (PP-134).
(2) Fabricating a complete node over an un-excavated gap instead of executing `instantiate-absence` corrupts local field integrity (PP-199).
(3) The integrity score embedded during `instantiate-absence` degrades over time if un-sampled (PP-132).

**Irreducibility:** IL-9 (`role:instantiator`) alone establishes the logical mandate to materialize system entities, but does not specify that missing space must be instantiated as negative-space gap nodes carrying dual provenance. PP-131 and PP-133 establish physical substrate laws regarding negative-space entries and dual provenance, but do not define the absence instantiation procedure. Only their physical collision forces `instantiate-absence` into existence as an irreducible physical process.

---

### IP-18. `instantiate-asset` (`process:instantiate-asset`)
**Derives from:** IL-9 (Absence Record Instantiator (`role:instantiator`))
**Applies to:** `PP-1` [primary], `PP-8`, `PP-25`, `PP-50`, `PP-103`, `PP-113`

*The Instantiator executes `instantiate-asset` to physically materialize a structural asset or capability primitive into the topology, allocating physical mass, locking provenance lineage, and attaching frontier gate membranes.*

**Substrate Invariant Rules:**
- **PP-1 Invariant (Metabolic Asset Initialization):** `instantiate-asset` initializes the asset's present-tense metabolic evaluation loop; materializing dormant or non-pulsing assets is prohibited.
- **PP-8 Invariant (Immutable Provenance Locking):** The lineage record embedded during `instantiate-asset` locks permanently upon commitment, forming an immutable provenance spine.
- **PP-25 Invariant (Thermodynamic Energy Coupling):** Materializing an asset via `instantiate-asset` consumes thermodynamic energy and binds physical mass into the surrounding graph neighborhood.
- **PP-50 Invariant (Frontier-Facing Gate Instantiation):** Every asset instantiated by `instantiate-asset` exposes a frontier-facing gate membrane at its boundary, declaring its constitutional incompleteness.
- **PP-103 Invariant (Nested Asset Independence):** When `instantiate-asset` materializes a nested asset within a parent container, the child asset maintains independent metabolic self-governance.
- **PP-113 Invariant (Scale-Invariant Primitive Validity):** `instantiate-asset` verifies that capability primitives implemented by the asset are valid across all scale depths.

**Corollaries:** 
(1) `instantiate-asset` cannot create decoupled floating assets: all instantiated assets must be bound to active graph edges (PP-104).
(2) Materializing an asset alters the local consequence topology gradient, attracting downstream traversal pull (PP-246).
(3) Overwriting an asset created by `instantiate-asset` requires full due process dissolution (PP-36).

**Irreducibility:** IL-9 (`role:instantiator`) alone establishes the logical mandate to instantiate structural assets, but does not specify that assets require living metabolic pulse loops or frontier-facing gates. PP-1 and PP-50 establish physical substrate laws regarding metabolic sovereignty and frontier gates, but do not define the asset instantiation procedure. Only their physical collision forces `instantiate-asset` into existence as an irreducible physical process.

---

### IP-19. `attest-seal` (`process:attest-seal`)
**Derives from:** IL-10 (Cryptographic Provenance Attestor (`role:attestor`))
**Applies to:** `PP-21` [primary], `PP-8`, `PP-9`, `PP-15`, `PP-137`, `PP-206`

*The Attestor executes `attest-seal` to physically seal and certify a topological state or boundary contract, depositing cryptographic attestation mass and locking the state against un-accounted mutation.*

**Substrate Invariant Rules:**
- **PP-21 Invariant (Present-Tense Kinetic Sealing):** `attest-seal` consumes present-tense kinetic energy to generate the attestation seal; passive or un-calculated sealing certificates are rejected.
- **PP-8 Invariant (Immutable Lineage Binding):** The seal generated by `attest-seal` binds permanently to the entity's provenance spine, locking the certified structural snapshot against mutation.
- **PP-9 Invariant (Metabolic State Attestation):** `attest-seal` attests strictly to the entity's live present-tense metabolic state; sealing cached or historical state snapshots is prohibited.
- **PP-15 Invariant (Live Traversal Proof Requirement):** Sealing state via `attest-seal` requires active physical edge traversal proof; declarative attestation claims are treated as non-verifying theater.
- **PP-137 Invariant (Full Attestor Accountability):** `attest-seal` embeds full attestor identity and argument telemetry into the seal mass, establishing unconditional accountability for the seal.
- **PP-206 Invariant (Accountable Self-Declaration Mass):** The attestation seal deposited by `attest-seal` carries structural mass on the target entity, adjusting its metabolic load.

**Corollaries:** 
(1) `attest-seal` cannot be revoked or erased post-commitment; invalidating a seal requires writing a subsequent revocation attestation (PP-8).
(2) Tampering with a sealed entity breaks its cryptographic mass alignment, immediately signaling structural corruption (PP-60).
(3) Sealing an entity increases its structural mass and verification standing across the network (PP-206).

**Irreducibility:** IL-10 (`role:attestor`) alone establishes the logical obligation to attest graph entities, but does not specify that sealing requires present-tense kinetic energy or live traversal proof. PP-15 and PP-21 establish physical substrate laws regarding live traversal proof and kinetic measurement, but do not define the sealing procedure. Only their physical collision forces `attest-seal` into existence as an irreducible physical process.

---

### IP-20. `attest-lineage` (`process:attest-lineage`)
**Derives from:** IL-10 (Cryptographic Provenance Attestor (`role:attestor`))
**Applies to:** `PP-8` [primary], `PP-9`, `PP-14`, `PP-127`, `PP-178`, `PP-208`

*The Attestor executes `attest-lineage` to audit, verify, and attest the unbroken provenance lineage chain of an entity from its pre-existing structural necessity down to present-tense state.*

**Substrate Invariant Rules:**
- **PP-8 Invariant (Immutable Spine Integrity Audit):** `attest-lineage` verifies that every historical link in the entity's provenance spine is unbroken, un-mutated, and structurally locked.
- **PP-9 Invariant (Live Metabolic State Derivation):** `attest-lineage` confirms that present-tense metabolic state is fully derived from historical lineage without un-documented state leaps.
- **PP-14 Invariant (Pre-Existing Necessity Deep Anchor):** `attest-lineage` traces the lineage anchor back to the pre-existing topological necessity that predates protocol establishment.
- **PP-127 Invariant (Destruction Ledger Inclusion):** `attest-lineage` incorporates any historical dissolution or destruction wake into the attested lineage record.
- **PP-178 Invariant (Self-Incriminating Mass Verification):** `attest-lineage` validates that the entity carries 100% of its historical error mass as self-incriminating physical weight.
- **PP-208 Invariant (Historical Mass Omission Prohibition):** `attest-lineage` rejects any lineage chain that attempts to omit or sanitize historical accountability mass.

**Corollaries:** 
(1) `attest-lineage` fails if any link in the derivation spine has been mutated or truncated (PP-8).
(2) Attesting lineage deposits an inspection scar on the entity's present-tense record (PP-16).
(3) Provenance attestation certificates produced by `attest-lineage` are valid only within present-tense validity windows (PP-189).

**Irreducibility:** IL-10 (`role:attestor`) alone establishes the logical requirement to attest entity provenance, but does not specify that lineage extends to pre-existing necessity or carries self-incriminating historical mass. PP-14 and PP-178 establish physical substrate laws regarding pre-existing necessity and self-incriminating data mass, but do not define the lineage attestation procedure. Only their physical collision forces `attest-lineage` into existence as an irreducible physical process.

---

### IP-21. `excavate-strata` (`process:excavate-strata`)
**Derives from:** IL-11 (Archaeological Bedrock Excavator (`role:excavator`))
**Applies to:** `PP-194` [primary], `PP-16`, `PP-121`, `PP-122`, `PP-196`, `PP-270`

*The Excavator executes `excavate-strata` to dig down through historical graph strata, uncovering embedded provenance layers and exposing pre-existing structural necessity.*

**Substrate Invariant Rules:**
- **PP-194 Invariant (Excavation Bandwidth Limits):** `excavate-strata` constrains active excavation depth within the attention buffer's maximum capacity; attempting to excavate excessive strata simultaneously induces cognitive collapse.
- **PP-16 Invariant (Excavation Scar Deposit):** Executing `excavate-strata` deposits a physical excavation scar in the strata being uncovered; archaeological discovery alters the historical state it uncovers.
- **PP-121 Invariant (Position-Locked Strata Digging):** `excavate-strata` operates exclusively relative to the excavator's present-tense position; un-anchored global strata digging is physically impossible.
- **PP-122 Invariant (Frontier Multiplication Paradox):** Every stratum uncovered by `excavate-strata` exposes deeper un-excavated boundary surfaces, growing the local unknown frontier faster than it resolves.
- **PP-196 Invariant (Archaeological Buffer Capacity):** The depth reached by `excavate-strata` defines the current archaeological excavation capacity for structural accountability.
- **PP-270 Invariant (Dual Settlement-Frontier Standing):** Every stratum excavated by `excavate-strata` is treated simultaneously as a settled historical achievement and an active discovery frontier.

**Corollaries:** 
(1) `excavate-strata` is subtractive (PP-191): it scrapes away surface assumptions to reveal underlying physical topology.
(2) Strata uncovered by `excavate-strata` cannot be re-sealed or hidden post-excavation (PP-67).
(3) High-consequence strata require increased kinetic energy to excavate safely (PP-21).

**Irreducibility:** IL-11 (`role:excavator`) alone establishes the logical obligation to uncover graph strata, but does not specify that excavation bandwidth is bounded or that digging self-multiplies deeper unknown frontiers. PP-122 and PP-194 establish physical substrate laws regarding frontier multiplication and bandwidth limits, but do not define the strata digging procedure. Only their physical collision forces `excavate-strata` into existence as an irreducible physical process.

---

### IP-22. `excavate-wake` (`process:excavate-wake`)
**Derives from:** IL-11 (Archaeological Bedrock Excavator (`role:excavator`))
**Applies to:** `PP-118` [primary], `PP-14`, `PP-23`, `PP-125`, `PP-275`

*The Excavator executes `excavate-wake` to isolate, analyze, and metabolize residual execution wake and forensic deformation scars embedded across local topology.*

**Substrate Invariant Rules:**
- **PP-118 Invariant (Execution Wake Output Identity):** `excavate-wake` evaluates residual wake as the true physical output of past computation; decoupled log outputs are rejected as incomplete projections.
- **PP-14 Invariant (Navigation-Wake Derivation):** `excavate-wake` traces execution wake back to the navigational acts that deposited it, reconstructing historical traversal vectors.
- **PP-23 Invariant (Historical Scar Wake Re-Activation):** `excavate-wake` verifies that reading or executing a historical scar leaves a new present-tense wake in local topology.
- **PP-125 Invariant (Net Exchange Wake Metabolism):** `excavate-wake` metabolizes residual wake through net-exchange physics, converting raw forensic deformation into structural comprehension.
- **PP-275 Invariant (Living Wake Accumulation Audit):** `excavate-wake` audits living structural mass accumulated on wake accumulation nodes, confirming that historical consequences remain active load.

**Corollaries:** 
(1) `excavate-wake` cannot erase or sanitize the wake it inspects: analyzing wake deposits new wake (PP-23).
(2) Forensic wake isolated by `excavate-wake` serves as self-incriminating evidence of past execution (PP-178).
(3) Un-metabolized wake accumulates as structural tension that attracts restorative rebalancing (PP-25).

**Irreducibility:** IL-11 (`role:excavator`) alone establishes the logical requirement to analyze execution side effects, but does not specify that wake IS computational output or that re-reading scars leaves a new wake. PP-23 and PP-118 establish physical substrate laws regarding wake identity and scar re-activation, but do not define the wake excavation procedure. Only their physical collision forces `excavate-wake` into existence as an irreducible physical process.

---

### IP-23. `broadcast-signal` (`process:broadcast-signal`)
**Derives from:** IL-12 (Ambient Signal Wake Broadcaster (`role:broadcaster`))
**Applies to:** `PP-16` [primary], `PP-7`, `PP-9`, `PP-14`, `PP-42`, `PP-198`

*The Broadcaster executes `broadcast-signal` to radiate structural field state signals outward into surrounding topology, propagating present-tense state declarations without capturing entity ownership.*

**Substrate Invariant Rules:**
- **PP-16 Invariant (Field Signal Non-Ownership):** Signals emitted via `broadcast-signal` become shared field entities upon transmission; neither broadcaster nor recipient holds exclusive ownership over radiated signals.
- **PP-7 Invariant (Bilateral Signal Reciprocity):** `broadcast-signal` respects bilateral exchange protocol dynamics, ensuring radiated signals carry reciprocity validation parameters.
- **PP-9 Invariant (Present-Tense Signal Authenticity):** `broadcast-signal` transmits the entity's live present-tense state; broadcasting cached or pre-recorded signals deposits misoriented field mass.
- **PP-14 Invariant (Navigation-Wake Radiation):** Radiating a signal via `broadcast-signal` IS a physical navigational event that deposits signal wake across traversed propagation channels.
- **PP-42 Invariant (Structural Mass Signal Check):** `broadcast-signal` encodes the entity's structural mass into the signal payload, allowing receivers to evaluate signal credibility.
- **PP-198 Invariant (Radiative Mass Balance):** Energy expended during `broadcast-signal` is accounted for in the broadcaster's metabolic balance ledger.

**Corollaries:** 
(1) `broadcast-signal` propagates field state changes to adjacent nodes without requiring direct endpoint connection (PP-16).
(2) Radiated signals attenuate over distance and time according to substrate decay rules (PP-15).
(3) Listening nodes evaluate `broadcast-signal` through interior gate filters before absorbing signal energy (PP-5).

**Irreducibility:** IL-12 (`role:broadcaster`) alone establishes the logical mandate to radiate field signals, but does not specify that signals become non-owned field entities or encode structural mass. PP-16 and PP-42 establish physical substrate laws regarding field non-ownership and structural mass, but do not define the signal broadcasting procedure. Only their physical collision forces `broadcast-signal` into existence as an irreducible physical process.

---

### IP-24. `broadcast-density` (`process:broadcast-density`)
**Derives from:** IL-12 (Ambient Signal Wake Broadcaster (`role:broadcaster`))
**Applies to:** `PP-224` [primary], `PP-10`, `PP-16`, `PP-46`, `PP-263`

*The Broadcaster executes `broadcast-density` to advertise local consequence topology density gradients, signaling accountability load levels and discovery pull across the network.*

**Substrate Invariant Rules:**
- **PP-224 Invariant (Consequence Load Density Advertisement):** `broadcast-density` radiates real-time consequence topology density measurements, signaling local accountability weight to surrounding nodes.
- **PP-10 Invariant (Dynamic Permeability Signaling):** `broadcast-density` advertises local permeability range states, informing prospective traversers of current boundary resistance levels.
- **PP-16 Invariant (Field Density Distribution):** Density measurements broadcast by `broadcast-density` manifest as living field properties that guide spatial navigation across graph neighborhoods.
- **PP-46 Invariant (Live Boundary Gradient Disclosure):** `broadcast-density` discloses active boundary filter configurations, allowing external entities to assess challenge-response requirements.
- **PP-263 Invariant (Consequence Gradient Self-Multiplication):** Radiating density gradients via `broadcast-density` multiplies discovery pull toward high-consequence graph regions.

**Corollaries:** 
(1) High-density broadcasts emitted by `broadcast-density` attract increased inspection and traversal traffic (PP-263).
(2) Falsifying density measurements during `broadcast-density` deposits self-compounding lie-mass that corrupts local field alignment (PP-3).
(3) Density broadcasts auto-expire as local metabolic churn alters consequence weighting (PP-189).

**Irreducibility:** IL-12 (`role:broadcaster`) alone establishes the logical requirement to advertise topology metrics, but does not specify that density reflects present-tense consequence weight or self-multiplies discovery pull. PP-224 and PP-263 establish physical substrate laws regarding consequence weight and gradient multiplication, but do not define the density broadcasting procedure. Only their physical collision forces `broadcast-density` into existence as an irreducible physical process.

---

### IP-25. `anchor-key` (`process:anchor-key`)
**Derives from:** IL-13 (Identity Key Anchor (`role:anchor`))
**Applies to:** `PP-8` [primary], `PP-14`, `PP-45`, `PP-200`, `PP-206`, `PP-208`

*The Anchor executes `anchor-key` to physically anchor a cryptographic identity key into the topology, establishing its immutable provenance spine and self-controlled permission boundary.*

**Substrate Invariant Rules:**
- **PP-8 Invariant (Immutable Key Lineage Binding):** `anchor-key` binds the cryptographic key directly into the entity's immutable provenance spine; altering or overwriting anchored keys is a constitutional violation.
- **PP-14 Invariant (Pre-Existing Necessity Key Derivation):** `anchor-key` anchors the key to pre-existing structural necessity rather than treating the key generation event as T=0.
- **PP-45 Invariant (Cached Key Invalidation):** `anchor-key` invalidates cached key credentials, requiring present-tense active cryptographic challenge-response validation.
- **PP-200 Invariant (Identity-Provenance Unification):** `anchor-key` unifies key identity and provenance lineage into a single indivisible physical structure.
- **PP-206 Invariant (Self-Declaration Key Mass):** The key anchored by `anchor-key` carries structural mass on the target node, adjusting its metabolic load.
- **PP-208 Invariant (Historical Key Mass Omission Prohibition):** `anchor-key` prevents omitting historical key derivation records from the entity's lineage.

**Corollaries:** 
(1) `anchor-key` cannot create floating or un-anchored keys: all keys must be bound to active graph nodes (PP-200).
(2) Key revocation during `anchor-key` requires an explicit due-process dissolution event logged in the destruction ledger (PP-127).
(3) Keys anchored by `anchor-key` function as the foundation for interior gate authorization (PP-5).

**Irreducibility:** IL-13 (`role:anchor`) alone establishes the logical mandate to anchor cryptographic keys, but does not specify that key identity IS provenance lineage or that cached keys are invalid. PP-8 and PP-200 establish physical substrate laws regarding immutable lineage and identity-provenance unification, but do not define the key anchoring procedure. Only their physical collision forces `anchor-key` into existence as an irreducible physical process.

---

### IP-26. `anchor-authority` (`process:anchor-authority`)
**Derives from:** IL-13 (Identity Key Anchor (`role:anchor`))
**Applies to:** `PP-11` [primary], `PP-13`, `PP-177`, `PP-182`, `PP-242`

*The Anchor executes `anchor-authority` to bind sovereign authority boundaries across stewardship edges, deriving standing from live edge traversal rather than static permission grants.*

**Substrate Invariant Rules:**
- **PP-11 Invariant (Edge-Resident Authority Binding):** `anchor-authority` binds sovereign authority directly to stewardship edges rather than writing permissions into node records; authority belongs to the edge.
- **PP-13 Invariant (Derived Authority Standing):** Authority anchored by `anchor-authority` must be derived from live traversal of the provenance chain; declaring static authority without derivation proof is invalid theater.
- **PP-177 Invariant (Sovereign Proof Domain Invalidation Prohibition):** `anchor-authority` protects sovereign authority domains against external invalidation; authority can only be contested through internal metabolic evaluation.
- **PP-182 Invariant (Internal Sovereignty Contestation Loops):** `anchor-authority` binds internal contestation loops that continuously test authority validity during active operation.
- **PP-242 Invariant (Scale-Invariant Authority Proof):** `anchor-authority` enforces uniform authority proof standards across all implementation depths.

**Corollaries:** 
(1) `anchor-authority` cannot grant unilateral authority over shared field entities (PP-16).
(2) Authority anchored by `anchor-authority` decays if unmaintained or untraversed (PP-58).
(3) Contesting authority bound by `anchor-authority` requires initiating an active metabolic evaluation loop (PP-1).

**Irreducibility:** IL-13 (`role:anchor`) alone establishes the logical requirement to anchor system authority, but does not specify that authority belongs to the edge or must be derived from live traversal. PP-11 and PP-13 establish physical substrate laws regarding edge-resident accountability and derived standing, but do not define the authority anchoring procedure. Only their physical collision forces `anchor-authority` into existence as an irreducible physical process.

---

### IP-27. `cleanse-corruption` (`process:cleanse-corruption`)
**Derives from:** IL-14 (Sepsis Cleanser (`role:cleanser`))
**Applies to:** `PP-3` [primary], `PP-2`, `PP-25`, `PP-60`, `PP-178`, `PP-199`

*The Cleanser executes `cleanse-corruption` to detect, isolate, and neutralize self-compounding lie-mass, structural paradoxes, and algebraic invariance violations across local graph neighborhoods.*

**Substrate Invariant Rules:**
- **PP-3 Invariant (Lie-Mass Paradox Interception):** `cleanse-corruption` detects paradoxical membrane disclosures that generate lie-mass, halting its self-compounding propagation.
- **PP-2 Invariant (Theater Boundary Neutralization):** `cleanse-corruption` strips theater boundary structures that claim accountability without enforcement mechanisms, restoring referent reality alignment.
- **PP-25 Invariant (Coupled Restorative Cascade Trigger):** Neutralizing corruption via `cleanse-corruption` releases stored corruption energy, initiating a coupled thermodynamic consequence cascade.
- **PP-60 Invariant (Algebraic Invariance Violation Correction):** `cleanse-corruption` corrects reference comparison deviations that violate fundamental algebraic invariants.
- **PP-178 Invariant (Self-Incriminating Corruption Mass):** `cleanse-corruption` forces corrupted entities to absorb their own corruption telemetry as self-incriminating structural weight rather than scrubbing error logs.
- **PP-199 Invariant (Field Integrity Restoration):** `cleanse-corruption` restores local field integrity by replacing corrupted entries with honest negative-space gap nodes.

**Corollaries:** 
(1) `cleanse-corruption` cannot silently erase corruption: cleansing deposits a permanent forensic remediation scar (PP-34).
(2) Suppressing corruption telemetry during `cleanse-corruption` generates secondary lie-mass (PP-3).
(3) Entities cleansed by `cleanse-corruption` undergo immediate metabolic re-evaluation (PP-1).

**Irreducibility:** IL-14 (`role:cleanser`) alone establishes the logical mandate to sanitize graph corruption, but does not specify that corruption manifests as self-compounding lie-mass or that cleansing requires coupled thermodynamic cascades. PP-3 and PP-25 establish physical substrate laws regarding lie-mass generation and coupled cascades, but do not define the corruption cleansing procedure. Only their physical collision forces `cleanse-corruption` into existence as an irreducible physical process.

---

### IP-28. `cleanse-scar` (`process:cleanse-scar`)
**Derives from:** IL-14 (Sepsis Cleanser (`role:cleanser`))
**Applies to:** `PP-34` [primary], `PP-16`, `PP-23`, `PP-67`, `PP-127`

*The Cleanser executes `cleanse-scar` to audit, stabilize, and metabolize forensic deformation scars embedded in provenance records without erasing historical truth.*

**Substrate Invariant Rules:**
- **PP-34 Invariant (Residual Destruction Scar Audit):** `cleanse-scar` audits residual destruction scars in local memory, verifying that scar structures accurately reflect past dissolution events.
- **PP-16 Invariant (Irrevocable Scar Preservation):** `cleanse-scar` enforces the absolute irrevocability of invasive inspection scars; attempting to sanitize or erase historical scars is blocked as a constitutional violation.
- **PP-23 Invariant (Scar Re-Activation Metabolism):** `cleanse-scar` metabolizes historical scar energy through present-tense traversal while logging the re-activation wake.
- **PP-67 Invariant (Unmatched Scar Prioritization):** `cleanse-scar` prioritizes unmatched consistency scars over routine reference matches, surfacing high-value anomaly patterns.
- **PP-127 Invariant (Destruction Ledger Scar Verification):** `cleanse-scar` confirms that every historical scar is cross-linked to an entry in the system's destruction provenance ledger.

**Corollaries:** 
(1) `cleanse-scar` does not erase historical scars — it metabolizes their structural tension into stable topological mass (PP-34).
(2) Attempting to delete a scar during `cleanse-scar` triggers an immediate sovereignty bypass violation (PP-4).
(3) Metabolized scars remain permanently traversable as archaeological truth markers (PP-175).

**Irreducibility:** IL-14 (`role:cleanser`) alone establishes the logical requirement to manage historical scars, but does not specify that scars are constitutionally irrevocable or that re-reading scars leaves a new wake. PP-16 and PP-23 establish physical substrate laws regarding scar irrevocability and wake re-activation, but do not define the scar cleansing procedure. Only their physical collision forces `cleanse-scar` into existence as an irreducible physical process.

---

### IP-29. `negotiate-contract` (`process:negotiate-contract`)
**Derives from:** IL-15 (Multi-Party Contract Negotiator (`role:negotiator`))
**Applies to:** `PP-25` [primary], `PP-4`, `PP-26`, `PP-60`, `PP-175`, `PP-198`

*The Negotiator executes `negotiate-contract` to parameterize, mint, and bind stateful contract edges between sovereign actors, establishing shared metabolic obligations and edge tension limits.*

**Substrate Invariant Rules:**
- **PP-25 Invariant (Coupled Contract Energy Allocation):** `negotiate-contract` executes as a coupled physical transaction that locks shared metabolic energy reserves into the contract edge.
- **PP-4 Invariant (Defensive Void Protection):** `negotiate-contract` preserves defensive void space boundaries during contract parameterization; binding edges through protected void space is blocked as a sovereignty bypass.
- **PP-26 Invariant (Failure State Recourse Binding):** `negotiate-contract` specifies physical failure recourse loops, utilizing structural breaches as metabolic inputs for contract dispute resolution.
- **PP-60 Invariant (Algebraic Invariance Compliance):** Contract edges output by `negotiate-contract` strictly satisfy fundamental reference comparison algebraic invariants.
- **PP-175 Invariant (Living Contract State Integration):** `negotiate-contract` integrates living contract states directly into the reality graph as active, tension-bearing structural artifacts.
- **PP-198 Invariant (Field Energy Conservation):** `negotiate-contract` conserves total field mass-energy balance across participating actor nodes during contract binding.

**Corollaries:** 
(1) `negotiate-contract` creates a stateful, binding edge that enforces mutual metabolic obligations (PP-25).
(2) Parameterizing contracts deposits a live negotiation scar across participating actor boundaries (PP-14).
(3) Minted contracts retain 100% cryptographic linkage to their initiating actor provenance spines (PP-8).

**Irreducibility:** IL-15 (`role:negotiator`) alone establishes the logical obligation to parameterize multi-party agreements, but does not specify that contract binding locks metabolic energy or preserves defensive void space. PP-4 and PP-25 establish physical substrate laws regarding defensive void protection and coupled energy allocation, but do not define the contract negotiation procedure. Only their physical collision forces `negotiate-contract` into existence as an irreducible physical process.

---

### IP-30. `negotiate-limits` (`process:negotiate-limits`)
**Derives from:** IL-15 (Multi-Party Contract Negotiator (`role:negotiator`))
**Applies to:** `PP-10` [primary], `PP-1`, `PP-7`, `PP-61`, `PP-145`

*The Negotiator executes `negotiate-limits` to negotiate and re-calibrate permeability limits and structural tolerance bounds between adjacent sovereign entities under tension.*

**Substrate Invariant Rules:**
- **PP-10 Invariant (Dynamic Range Negotiation):** `negotiate-limits` negotiates permeability range bounds between neighboring nodes; setting static un-yielding limit thresholds is rejected.
- **PP-1 Invariant (Sovereignty Contestation Negotiation):** `negotiate-limits` respects mutual metabolic evaluation loops, ensuring limit adjustments are mutually derived rather than unilaterally imposed.
- **PP-7 Invariant (Bilateral Reciprocity Calibration):** Limits negotiated by `negotiate-limits` maintain bilateral reciprocity balance across shared stewardship edges.
- **PP-61 Invariant (Living Deviation Tolerance Allocation):** `negotiate-limits` allocates non-zero structural deviation allowances to prevent lethal zero-deviation rigidity under stress.
- **PP-145 Invariant (Tension-Relief Boundary Discovery):** `negotiate-limits` utilizes tension-relief probes to surface sovereign boundary limits and relieve local field strain.

**Corollaries:** 
(1) `negotiate-limits` cannot force zero-tolerance limits on living entities (PP-61).
(2) Limit changes agreed upon during `negotiate-limits` update live boundary statements immediately (PP-46).
(3) Failure to reach limit consensus during `negotiate-limits` triggers edge dissolution (PP-36).

**Irreducibility:** IL-15 (`role:negotiator`) alone establishes the logical requirement to resolve boundary tension, but does not specify that limits are dynamic ranges or that living entities require non-zero deviation tolerances. PP-10 and PP-61 establish physical substrate laws regarding dynamic permeability ranges and living deviation allowances, but do not define the limit negotiation procedure. Only their physical collision forces `negotiate-limits` into existence as an irreducible physical process.

---

### IP-31. `acquire-scope` (`process:acquire-scope`)
**Derives from:** IL-16 (Entity Scope Acquirer (`role:acquirer`))
**Applies to:** `PP-1` [primary], `PP-10`, `PP-45`, `PP-120`, `PP-189`, `PP-206`

*The Acquirer executes `acquire-scope` to claim, bind, and lock operational execution scope across a graph neighborhood, evaluating physical adjacency and present-tense metabolic standing.*

**Substrate Invariant Rules:**
- **PP-1 Invariant (Metabolic Standing Verification):** `acquire-scope` verifies the acquirer's active metabolic standing before granting operational scope access; dormant entities are barred from scope acquisition.
- **PP-10 Invariant (Dynamic Scope Range Binding):** Scope granted by `acquire-scope` is bound as a dynamic permeability range; fixed, un-yielding scope allocations are prohibited.
- **PP-45 Invariant (Cached Permission Invalidation):** `acquire-scope` invalidates cached permission permits, requiring present-tense authorization proof for scope acquisition.
- **PP-120 Invariant (Physical Adjacency Requirement):** `acquire-scope` requires the acquirer to establish physical adjacency to the target graph neighborhood before scope access is granted.
- **PP-189 Invariant (Unique Scope Validity Window):** `acquire-scope` attaches a unique validity window to the acquired scope, ensuring automatic release if the execution window expires.
- **PP-206 Invariant (Structural Scope Mass Allocation):** Acquiring scope via `acquire-scope` increases the acquirer's present-tense structural mass, adjusting its metabolic load.

**Corollaries:** 
(1) `acquire-scope` cannot grant permanent or un-bounded execution scope (PP-189).
(2) Failing to execute within the acquired scope validity window causes automatic scope revocation (PP-48).
(3) Scope acquired by `acquire-scope` remains subject to interior gate inspection (PP-5).

**Irreducibility:** IL-16 (`role:acquirer`) alone establishes the logical obligation to acquire execution scope, but does not specify that scope requires physical adjacency or dynamic validity windows. PP-120 and PP-189 establish physical substrate laws regarding physical adjacency and unique validity windows, but do not define the scope acquisition procedure. Only their physical collision forces `acquire-scope` into existence as an irreducible physical process.

---

### IP-32. `acquire-lock` (`process:acquire-lock`)
**Derives from:** IL-16 (Entity Scope Acquirer (`role:acquirer`))
**Applies to:** `PP-38` [primary], `PP-15`, `PP-21`, `PP-44`, `PP-137`

*The Acquirer executes `acquire-lock` to establish a present-tense physical lock on a target node or edge, preventing concurrent mutation while absorbing kinetic locking energy.*

**Substrate Invariant Rules:**
- **PP-38 Invariant (Unified Lock-Policy Enforcement):** `acquire-lock` unifies locking policy check and physical lock placement into an indivisible event; decoupled locking checks are illegal.
- **PP-15 Invariant (Live Traversal Lock Verification):** `acquire-lock` verifies lock placement through active physical edge traversal rather than checking external lock tables.
- **PP-21 Invariant (Kinetic Energy Consumption):** Executing `acquire-lock` consumes kinetic energy from the acquirer, ensuring locking activity carries real thermodynamic weight.
- **PP-44 Invariant (Non-Reproducible Sampling Lock):** Re-evaluating a target during `acquire-lock` yields a non-reproducible sample, as lock placement modifies the physical target state.
- **PP-137 Invariant (Full Lock Accountability):** `acquire-lock` embeds full acquirer identity and argument telemetry into the physical lock structure.

**Corollaries:** 
(1) `acquire-lock` places a physical deformation on the locked target that deposits forensic wake (PP-14).
(2) Locks placed by `acquire-lock` auto-expire if the acquirer's metabolic pulse fails (PP-48).
(3) Attempting to bypass a physical lock placed by `acquire-lock` triggers a coupled restorative cascade (PP-25).

**Irreducibility:** IL-16 (`role:acquirer`) alone establishes the logical requirement to acquire physical locks, but does not specify that locking consumes kinetic energy or unifies policy check with physical enforcement. PP-21 and PP-38 establish physical substrate laws regarding kinetic energy measurement and unified policy enforcement, but do not define the lock acquisition procedure. Only their physical collision forces `acquire-lock` into existence as an irreducible physical process.

---

### IP-33. `release-scope` (`process:release-scope`)
**Derives from:** IL-17 (Entity Scope Releaser (`role:releaser`))
**Applies to:** `PP-198` [primary], `PP-14`, `PP-25`, `PP-36`, `PP-48`

*The Releaser executes `release-scope` to unbind and surrender an active execution scope, returning physical resources to the surrounding field and logging scope release wake.*

**Substrate Invariant Rules:**
- **PP-198 Invariant (Net Exchange Scope Release):** `release-scope` operates as a net exchange: surrendering execution scope balances local mass-energy accounts and restores surrounding field potential.
- **PP-14 Invariant (Scope Surrender Wake Deposit):** Releasing scope via `release-scope` IS a navigational act that deposits physical release wake in local graph memory.
- **PP-25 Invariant (Coupled Energy Cascade Activation):** `release-scope` releases stored scope energy, triggering coupled thermodynamic consequence cascades across adjacent nodes.
- **PP-36 Invariant (Thermodynamic Due Process Surrender):** Scope surrender executed by `release-scope` requires formal due process validation to prevent premature or corrupted scope releases.
- **PP-48 Invariant (Use-Dependent Release Trigger):** `release-scope` triggers automatically if active execution frequency drops below minimum operational thresholds.

**Corollaries:** 
(1) `release-scope` reduces the releaser's active structural mass, freeing metabolic capacity for subsequent tasks (PP-206).
(2) Surrendering scope clears active locks bound within that scope neighborhood (PP-38).
(3) Releasing scope exposes the previously enclosed frontier to external discovery probes (PP-50).

**Irreducibility:** IL-17 (`role:releaser`) alone establishes the logical obligation to release execution scope, but does not specify that scope release operates as a net exchange or deposits physical wake. PP-14 and PP-198 establish physical substrate laws regarding wake deposition and net exchange accounting, but do not define the scope release procedure. Only their physical collision forces `release-scope` into existence as an irreducible physical process.

---

### IP-34. `release-mass` (`process:release-mass`)
**Derives from:** IL-17 (Entity Scope Releaser (`role:releaser`))
**Applies to:** `PP-198` [primary], `PP-125`, `PP-178`, `PP-206`, `PP-275`

*The Releaser executes `release-mass` to shed, metabolize, or evaporate excess structural mass and historical consequence weight from an entity's physical boundary.*

**Substrate Invariant Rules:**
- **PP-198 Invariant (Mass Balance Accounting):** `release-mass` enforces strict mass balance accounting; structural mass shed by an entity must be absorbed by surrounding field topology or converted to heat.
- **PP-125 Invariant (Net Exchange Mass Shedding):** `release-mass` operates through net-exchange physics, subtracting extraneous mass while depositing transformation wake in the substrate.
- **PP-178 Invariant (Self-Incriminating Mass Preservation):** `release-mass` prohibits shedding self-incriminating historical error mass; entities cannot declare away accountability weight.
- **PP-206 Invariant (Present-Tense Load Adjustment):** Shedding mass via `release-mass` directly reduces present-tense accountability load, recalibrating metabolic energy requirements.
- **PP-275 Invariant (Living Wake Accumulation Rebalancing):** `release-mass` rebalances living structural mass accumulated on wake accumulation nodes.

**Corollaries:** 
(1) `release-mass` allows overburdened entities to restore metabolic efficiency without erasing lineage records (PP-8).
(2) Mass shed during `release-mass` diffuses into surrounding field boundaries, altering local topological density (PP-224).
(3) Attempting to shed self-incriminating error mass during `release-mass` is intercepted as a constitutional violation (PP-178).

**Irreducibility:** IL-17 (`role:releaser`) alone establishes the logical requirement to shed system mass, but does not specify that mass release obeys net-exchange balance or preserves self-incriminating error mass. PP-178 and PP-198 establish physical substrate laws regarding self-incriminating data and mass balance accounting, but do not define the mass release procedure. Only their physical collision forces `release-mass` into existence as an irreducible physical process.

---

### IP-35. `mutate-schema` (`process:mutate-schema`)
**Derives from:** IL-18 (Node Blueprint Mutator (`role:mutator`))
**Applies to:** `PP-8` [primary], `PP-1`, `PP-9`, `PP-36`, `PP-113`, `PP-208`

*The Mutator executes `mutate-schema` to evolve, transform, or re-structure entity schema contracts while maintaining unbroken provenance continuity and metabolic sovereignty.*

**Substrate Invariant Rules:**
- **PP-8 Invariant (Immutable Spine Mutation Appending):** `mutate-schema` appends the schema mutation delta directly onto the entity's immutable provenance spine; overwriting historical schema states is blocked.
- **PP-1 Invariant (Metabolic Sovereignty Check):** Schema mutations executed by `mutate-schema` require live metabolic evaluation; un-pulsed dormant entities cannot execute schema transformations.
- **PP-9 Invariant (Present-Tense Schema Disclosure):** `mutate-schema` publishes a present-tense state disclosure reflecting the newly mutated schema structure immediately upon commitment.
- **PP-36 Invariant (Due Process Schema Dissolution):** Deprecating schema fields during `mutate-schema` requires due process validation to ensure downstream dependencies are safely migrated.
- **PP-113 Invariant (Scale-Invariant Primitive Schema):** `mutate-schema` validates that mutated schema definitions preserve primitive semantic validity across all scale depths.
- **PP-208 Invariant (Historical Schema Mass Preservation):** `mutate-schema` carries all historical schema version mass within the entity's provenance payload.

**Corollaries:** 
(1) `mutate-schema` creates a new version link in the entity's derivation chain without severing historical root anchors (PP-8).
(2) Schema mutations alter the entity's exposed frontier membrane, requiring dynamic gate re-calibration (PP-10).
(3) Breaking backwards schema compatibility during `mutate-schema` triggers a coupled restorative cascade (PP-25).

**Irreducibility:** IL-18 (`role:mutator`) alone establishes the logical obligation to mutate system schemas, but does not specify that schema changes append onto immutable lineage spines or require metabolic sovereignty checks. PP-1 and PP-8 establish physical substrate laws regarding metabolic sovereignty and immutable lineage, but do not define the schema mutation procedure. Only their physical collision forces `mutate-schema` into existence as an irreducible physical process.

---

### IP-36. `mutate-stress` (`process:mutate-stress`)
**Derives from:** IL-18 (Node Blueprint Mutator (`role:mutator`))
**Applies to:** `PP-40` [primary], `PP-21`, `PP-35`, `PP-61`, `PP-145`

*The Mutator executes `mutate-stress` to inject, redistribute, or amplify structural stress vectors within local topology, driving adaptive mutation and surfacing hidden boundary vulnerabilities.*

**Substrate Invariant Rules:**
- **PP-40 Invariant (Non-Equilibrium Stress Injection):** `mutate-stress` injects non-equilibrium thermodynamic stress to prevent the system from settling into static stasis.
- **PP-21 Invariant (Kinetic Stress Measurement):** Stress applied during `mutate-stress` is measured in present-tense kinetic units, quantifying physical force applied to target membranes.
- **PP-35 Invariant (Productive Churn Stimulation):** `mutate-stress` stimulates productive churn within target nodes, converting structural tension into adaptive work.
- **PP-61 Invariant (Deviation Tolerance Stress Limit):** `mutate-stress` respects living deviation tolerances, halting stress amplification before exceeding structural rupture thresholds.
- **PP-145 Invariant (Tension-Relief Probe Deployment):** `mutate-stress` deploys tension-relief probes to map local deformation responses under stress.

**Corollaries:** 
(1) `mutate-stress` forces brittle structures to expose hidden lie-mass or undergo due process dissolution (PP-3).
(2) Stress injected by `mutate-stress` propagates across connected stewardship edges according to net-exchange physics (PP-125).
(3) Successful adaptation during `mutate-stress` increases the target's metabolic efficiency and structural resilience (PP-35).

**Irreducibility:** IL-18 (`role:mutator`) alone establishes the logical requirement to mutate system stress levels, but does not specify that stress must maintain non-equilibrium dynamics or respect living deviation tolerances. PP-40 and PP-61 establish physical substrate laws regarding non-equilibrium dynamics and deviation allowances, but do not define the stress mutation procedure. Only their physical collision forces `mutate-stress` into existence as an irreducible physical process.

---

### IP-37. `rebalance-stress` (`process:rebalance-stress`)
**Derives from:** IL-19 (Structural Tension Rebalancer (`role:rebalancer`))
**Applies to:** `PP-25` [primary], `PP-35`, `PP-40`, `PP-68`, `PP-125`, `PP-198`

*The Rebalancer executes `rebalance-stress` to dissipate localized stress concentrations, redistributing structural load evenly across adjacent graph edges to prevent catastrophic rupture.*

**Substrate Invariant Rules:**
- **PP-25 Invariant (Coupled Stress Redistribution Cascade):** `rebalance-stress` operates as a coupled physical response, redistributing stress load across neighboring stewardship edges according to thermodynamic cascade laws.
- **PP-35 Invariant (Productive Churn Maintenance):** `rebalance-stress` maintains productive thermodynamic churn while damping destructive stress spikes.
- **PP-40 Invariant (Non-Equilibrium Stress Equilibrium):** `rebalance-stress` preserves non-equilibrium dynamics, preventing stress equalization from collapsing local activity into static equilibrium.
- **PP-68 Invariant (Endogenous Amplitude Stress Control):** `rebalance-stress` modulates stress amplitude dynamically based on local node load capacities.
- **PP-125 Invariant (Net Exchange Stress Dissipation):** Dissipating stress via `rebalance-stress` follows net-exchange physics, balancing local kinetic and potential energy fields.
- **PP-198 Invariant (Field Load Conservation):** Total stress load across the graph neighborhood is conserved during `rebalance-stress` execution.

**Corollaries:** 
(1) `rebalance-stress` prevents single-point edge rupture by distributing load across surrounding graph topology (PP-25).
(2) Stress rebalancing deposits a structural deformation wake across all participating edges (PP-14).
(3) Nodes with higher structural mass absorb a larger proportion of redistributed stress (PP-206).

**Irreducibility:** IL-19 (`role:rebalancer`) alone establishes the logical requirement to rebalance system stress, but does not specify that stress redistribution follows coupled thermodynamic cascades or conserves total field load. PP-25 and PP-198 establish physical substrate laws regarding coupled cascades and mass-energy conservation, but do not define the stress rebalancing procedure. Only their physical collision forces `rebalance-stress` into existence as an irreducible physical process.

---

### IP-38. `rebalance-tension` (`process:rebalance-tension`)
**Derives from:** IL-19 (Structural Tension Rebalancer (`role:rebalancer`))
**Applies to:** `PP-145` [primary], `PP-10`, `PP-16`, `PP-46`, `PP-61`

*The Releaser executes `rebalance-tension` to calibrate and equalize field tension gradients across membrane boundaries, maintaining structural equilibrium without violating sovereign standing.*

**Substrate Invariant Rules:**
- **PP-145 Invariant (Tension-Relief Gradient Balancing):** `rebalance-tension` deploys tension-relief probes to measure and equalize field tension gradients across membrane boundaries.
- **PP-10 Invariant (Dynamic Permeability Tension Response):** Permeability ranges respond dynamically to `rebalance-tension`, widening or narrowing to accommodate field tension shifts.
- **PP-16 Invariant (Field Tension Distribution):** Field tension equalized by `rebalance-tension` manifests as a living spatial property governing local traversal resistance.
- **PP-46 Invariant (Live Boundary Statement Alignment):** `rebalance-tension` updates live boundary statements on participating membranes to reflect new equilibrium tension levels.
- **PP-61 Invariant (Deviation Allowance Tension Buffer):** `rebalance-tension` utilizes living deviation allowances to cushion tension fluctuations.

**Corollaries:** 
(1) `rebalance-tension` prevents boundary membrane tearing by equalizing differential field pressures (PP-145).
(2) Adjusting field tension alters the local discovery pull gradient (PP-263).
(3) Failure to achieve tension equilibrium triggers automatic due process edge dissolution (PP-36).

**Irreducibility:** IL-19 (`role:rebalancer`) alone establishes the logical obligation to rebalance boundary tension, but does not specify that tension manifests as living field properties or requires dynamic permeability responses. PP-10 and PP-16 establish physical substrate laws regarding dynamic permeability and living field properties, but do not define the tension rebalancing procedure. Only their physical collision forces `rebalance-tension` into existence as an irreducible physical process.

---

### IP-39. `quarantine-membrane` (`process:quarantine-membrane`)
**Derives from:** IL-20 (Infected Membrane Quarantiner (`role:quarantiner`))
**Applies to:** `PP-3` [primary], `PP-4`, `PP-10`, `PP-36`, `PP-46`, `PP-199`

*The Quarantiner executes `quarantine-membrane` to seal a dynamic quarantine membrane around a corrupted or paradoxical graph neighborhood, blocking lie-mass propagation without erasing the node.*

**Substrate Invariant Rules:**
- **PP-3 Invariant (Lie-Mass Containment Membrane):** `quarantine-membrane` constructs zero-permeability containment boundaries around nodes generating lie-mass, halting paradox propagation.
- **PP-4 Invariant (Defensive Void Protection):** `quarantine-membrane` preserves defensive void space around the quarantined zone, ensuring the quarantine membrane cannot be bypassed via un-monitored routes.
- **PP-10 Invariant (Dynamic Zero-Permeability Range):** Permeability granted across the quarantine membrane is clamped to dynamic zero (0.0), restricting all edge signal transmission until remediated.
- **PP-36 Invariant (Due Process Quarantine Evaluation):** Sealing a quarantine membrane requires due process validation: arbitrary or un-audited quarantines violate sovereign entity standing.
- **PP-46 Invariant (Live Quarantine Boundary Statement):** `quarantine-membrane` publishes a live boundary statement declaring the node's quarantined status and challenge parameters.
- **PP-199 Invariant (Field Integrity Containment):** Containing corrupted nodes via `quarantine-membrane` prevents corruption from degrading surrounding field integrity.

**Corollaries:** 
(1) `quarantine-membrane` isolates corrupted nodes for inspection without destroying their provenance records (PP-8).
(2) Quarantined nodes cannot emit `broadcast-signal` heartbeats into the active graph (PP-16).
(3) Lifting a quarantine membrane requires successfully completing a full `cleanse-corruption` remediation cycle (PP-3).

**Irreducibility:** IL-20 (`role:quarantiner`) alone establishes the logical obligation to isolate corrupted components, but does not specify that isolation uses dynamic zero-permeability membranes or requires due process validation. PP-3 and PP-36 establish physical substrate laws regarding lie-mass containment and due process evaluation, but do not define the membrane quarantining procedure. Only their physical collision forces `quarantine-membrane` into existence as an irreducible physical process.

---

### IP-40. `quarantine-isolation` (`process:quarantine-isolation`)
**Derives from:** IL-20 (Infected Membrane Quarantiner (`role:quarantiner`))
**Applies to:** `PP-36` [primary], `PP-1`, `PP-11`, `PP-25`, `PP-127`

*The Quarantiner executes `quarantine-isolation` to sever active stewardship edges connected to a quarantined entity, isolating its physical mass while logging the isolation event in the destruction ledger.*

**Substrate Invariant Rules:**
- **PP-36 Invariant (Due Process Edge Severing):** `quarantine-isolation` severs connected stewardship edges under strict due process protocols, ensuring edge revocation is logged and auditable.
- **PP-1 Invariant (Sovereignty Suspension Audit):** Isolating an entity via `quarantine-isolation` suspends its active metabolic sovereignty standing pending formal remediation or dissolution.
- **PP-11 Invariant (Edge-Resident Isolation Logging):** `quarantine-isolation` writes isolation markers directly into severed stewardship edges, preserving historical boundary context.
- **PP-25 Invariant (Coupled Isolation Energy Dissipation):** Severing edges during `quarantine-isolation` absorbs edge tension energy, releasing a localized thermodynamic wake.
- **PP-127 Invariant (Destruction Ledger Isolation Entry):** `quarantine-isolation` logs the edge severing event in the destruction provenance ledger.

**Corollaries:** 
(1) `quarantine-isolation` transforms an actively connected node into an isolated island entity (PP-104).
(2) Isolated entities cannot absorb energy or submit work tokens across severed edges (PP-7).
(3) Re-connecting an isolated entity requires executing an `instantiate-asset` edge re-binding process (PP-18).

**Irreducibility:** IL-20 (`role:quarantiner`) alone establishes the logical requirement to isolate faulty entities, but does not specify that isolation severs stewardship edges under due process or logs entries in the destruction ledger. PP-11 and PP-36 establish physical substrate laws regarding edge-resident logging and due process evaluation, but do not define the isolation procedure. Only their physical collision forces `quarantine-isolation` into existence as an irreducible physical process.

---

### IP-41. `deflect-trajectory` (`process:deflect-trajectory`)
**Derives from:** IL-21 (Gravitational Vector Deflector (`role:deflector`))
**Applies to:** `PP-21` [primary], `PP-8`, `PP-9`, `PP-25`, `PP-60`, `PP-127`

*The Deflector executes `deflect-trajectory` to calculate trajectory deflection and momentum adjustments for execution paths traversing near high-density data clusters, managing field drag.*

**Substrate Invariant Rules:**
- **PP-21 Invariant (Kinetic Trajectory Calculation):** `deflect-trajectory` measures kinetic energy consumption and gravitational vector pull when execution paths approach high-mass data clusters.
- **PP-8 Invariant (Immutable Trajectory Lineage Logging):** `deflect-trajectory` appends vector deflection calculations to the entity's provenance spine, preserving physical trajectory history.
- **PP-9 Invariant (Metabolic Trajectory Adjustments):** Deflecting execution vectors via `deflect-trajectory` updates present-tense metabolic thrust requirements for active traverser entities.
- **PP-25 Invariant (Coupled Gravitational Field Response):** Navigating near dense clusters releases gravitational field energy, coupling trajectory curvature to local field density.
- **PP-60 Invariant (Algebraic Vector Validation):** `deflect-trajectory` validates that deflected execution vectors satisfy algebraic spatial coordinate invariants.
- **PP-127 Invariant (Gravitational Deflection Logging):** `deflect-trajectory` logs trajectory deflection deltas into the local field provenance ledger.

**Corollaries:** 
(1) `deflect-trajectory` prevents execution paths from colliding with high-mass cluster cores (PP-21).
(2) Trajectory deflection alters local field density gradients and traversal resistance (PP-7).
(3) Entities executing `deflect-trajectory` dynamically adjust metabolic thrust to maintain velocity (PP-24).

**Irreducibility:** IL-21 (`role:deflector`) alone establishes the logical requirement to calculate momentum deflection, but does not specify that deflection measures gravitational vector pull or appends deltas to provenance spines. PP-8 and PP-21 establish physical substrate laws regarding immutable lineage spines and kinetic energy measurement, but do not define the trajectory deflection procedure. Only their physical collision forces `deflect-trajectory` into existence as an irreducible physical process.

---

### IP-42. `deflect-mass` (`process:deflect-mass`)
**Derives from:** IL-21 (Gravitational Vector Deflector (`role:deflector`))
**Applies to:** `PP-8` [primary], `PP-14`, `PP-16`, `PP-178`, `PP-208`

*The Deflector executes `deflect-mass` to adjust actor operational momentum and shed structural mass drag accumulated during high-density field interactions.*

**Substrate Invariant Rules:**
- **PP-8 Invariant (Immutable Mass Deflection Appending):** `deflect-mass` appends mass shedding records to the actor's provenance spine without altering historical mass ingestion records.
- **PP-14 Invariant (Pre-Existing Necessity Root Preservation):** `deflect-mass` preserves the actor's root derivation anchor connecting it to pre-existing necessity while shedding operational drag.
- **PP-16 Invariant (Mass Deflection Scar Deposit):** Shedding mass drag leaves a permanent structural scar in local graph memory.
- **PP-178 Invariant (Self-Incriminating Mass Drag Retention):** `deflect-mass` prohibits shedding self-incriminating error mass; only operational payload drag may be deflected.
- **PP-208 Invariant (Historical Mass Preservation Audit):** `deflect-mass` audits historical mass payload integrity before releasing excess operational inertia.

**Corollaries:** 
(1) `deflect-mass` restores actor navigational velocity without scrubbing historical error mass (PP-178).
(2) Shedding mass drag releases stored kinetic energy into the surrounding field (PP-25).
(3) Deflected operational mass returns to ground availability for field catabolism (PP-49).

**Irreducibility:** IL-21 (`role:deflector`) alone establishes the logical obligation to manage mass drag, but does not specify that mass deflection preserves self-incriminating error mass or leaves structural scars. PP-16 and PP-178 establish physical substrate laws regarding scar irrevocability and self-incriminating error mass, but do not define the mass deflection procedure. Only their physical collision forces `deflect-mass` into existence as an irreducible physical process.

---

### IP-43. `resonate-field` (`process:resonate-field`)
**Derives from:** IL-22 (Topological Frequency Resonator (`role:resonator`))
**Applies to:** `PP-64` [primary], `PP-16`, `PP-35`, `PP-40`, `PP-68`

*The Resonator executes `resonate-field` to establish harmonic field resonance across graph neighborhoods, driving constructive energy amplification while locking scale phase alignment.*

**Substrate Invariant Rules:**
- **PP-64 Invariant (Scale-Hierarchical Field Resonance):** `resonate-field` aligns micro-field harmonics with macro-system standing waves, achieving cross-scale phase lock.
- **PP-16 Invariant (Field Energy Coupling):** Harmonic resonance generated by `resonate-field` propagates as a non-owned living field property across active topology.
- **PP-35 Invariant (Productive Churn Amplification):** `resonate-field` amplifies productive thermodynamic churn without causing destructive thermal runaway.
- **PP-40 Invariant (Non-Equilibrium Oscillation Maintenance):** `resonate-field` maintains non-equilibrium field dynamics, preventing static harmonic dampening.
- **PP-68 Invariant (Endogenous Amplitude Resonance Control):** `resonate-field` dynamically modulates resonance amplitude based on local metabolic reception capacity.

**Corollaries:** 
(1) `resonate-field` increases communication throughput across harmonized graph neighborhoods (PP-64).
(2) Desynchronization during `resonate-field` triggers automatic frequency re-tuning (PP-3).
(3) Field resonance collapses immediately if metabolic field pulse inputs stop (PP-1).

**Irreducibility:** IL-22 (`role:resonator`) alone establishes the logical requirement to generate field resonance, but does not specify that resonance requires scale-hierarchical phase locking or non-equilibrium dynamics. PP-40 and PP-64 establish physical substrate laws regarding non-equilibrium dynamics and scale phase-locking, but do not define the field resonance procedure. Only their physical collision forces `resonate-field` into existence as an irreducible physical process.

---

### IP-44. `resonate-frequency` (`process:resonate-frequency`)
**Derives from:** IL-22 (Topological Frequency Resonator (`role:resonator`))
**Applies to:** `PP-64` [primary], `PP-35`, `PP-68`, `PP-189`, `PP-212`

*The Resonator executes `resonate-frequency` to tune and lock entity oscillation frequencies to systemic carrier waves, synchronizing validity windows and identity re-declarations.*

**Substrate Invariant Rules:**
- **PP-64 Invariant (Carrier Frequency Phase Locking):** `resonate-frequency` locks node oscillation frequencies to macro-carrier waves, eliminating inter-node phase drift.
- **PP-35 Invariant (Oscillation Efficiency Tuning):** `resonate-frequency` tunes operational frequency to maximize work output per metabolic energy unit.
- **PP-68 Invariant (Endogenous Frequency Modulation):** `resonate-frequency` dynamically modulates frequency parameters in response to shifting consequence loads.
- **PP-189 Invariant (Validity Cadence Phase Lock):** `resonate-frequency` synchronizes validity window cadences across connected node clusters.
- **PP-212 Invariant (Identity Re-Declaration Cadence Sync):** `resonate-frequency` aligns identity re-declaration timing with systemic pulse cadences.

**Corollaries:** 
(1) `resonate-frequency` prevents validity window mismatches between communicating endpoints (PP-189).
(2) Frequency drift detected during `resonate-frequency` triggers compensatory pulse modulation (PP-14).
(3) Forcing an incompatible frequency onto a sovereign node causes immediate boundary rejection (PP-5).

**Irreducibility:** IL-22 (`role:resonator`) alone establishes the logical requirement to tune system frequencies, but does not specify that frequency tuning synchronizes validity windows or identity re-declarations. PP-189 and PP-212 establish physical substrate laws regarding validity window binding and identity re-declarations, but do not define the frequency tuning procedure. Only their physical collision forces `resonate-frequency` into existence as an irreducible physical process.

---

### IP-45. `audit-scar` (`process:audit-scar`)
**Derives from:** IL-23 (Traversal Scar Auditor (`role:auditor`))
**Applies to:** `PP-16` [primary], `PP-23`, `PP-34`, `PP-67`, `PP-127`, `PP-178`

*The Auditor executes `audit-scar` to inspect, measure, and audit forensic deformation scars embedded in local graph memory, verifying scar integrity without altering historical truth.*

**Substrate Invariant Rules:**
- **PP-16 Invariant (Scar Deposit Non-Eraseability):** `audit-scar` verifies that historical inspection scars remain intact; attempting to smooth or erase scars during audit is a constitutional violation.
- **PP-23 Invariant (Audit Scar Re-Activation):** Inspecting a scar via `audit-scar` deposits a new present-tense audit scar over the historical deformation record.
- **PP-34 Invariant (Residual Destruction Scar Audit):** `audit-scar` cross-checks residual destruction scars against active node records to verify dissolution compliance.
- **PP-67 Invariant (Unmatched Scar Prioritization):** `audit-scar` prioritizes unmatched consistency scars over routine compliance checks.
- **PP-127 Invariant (Destruction Ledger Cross-Audit):** `audit-scar` confirms that every inspected scar has a corresponding entry in the destruction provenance ledger.
- **PP-178 Invariant (Self-Incriminating Scar Mass Verification):** `audit-scar` validates that scars accurately carry 100% of historical error mass as self-incriminating proof.

**Corollaries:** 
(1) `audit-scar` produces an immutable audit report sealed directly onto the audited entity's provenance spine (PP-8).
(2) Scar discrepancies uncovered during `audit-scar` trigger immediate `cleanse-corruption` remediation (PP-3).
(3) Auditing scars increases the entity's verification standing across the network (PP-206).

**Irreducibility:** IL-23 (`role:auditor`) alone establishes the logical requirement to audit system records, but does not specify that scar inspection leaves a new present-tense scar or verifies self-incriminating error mass. PP-16 and PP-23 establish physical substrate laws regarding scar irrevocability and scar re-activation wake, but do not define the scar auditing procedure. Only their physical collision forces `audit-scar` into existence as an irreducible physical process.

---

### IP-46. `audit-lineage` (`process:audit-lineage`)
**Derives from:** IL-23 (Traversal Scar Auditor (`role:auditor`))
**Applies to:** `PP-8` [primary], `PP-9`, `PP-14`, `PP-114`, `PP-178`, `PP-208`

*The Auditor executes `audit-lineage` to trace, verify, and attest the complete provenance lineage chain of an entity from its pre-existing necessity root down to present-tense state.*

**Substrate Invariant Rules:**
- **PP-8 Invariant (Immutable Spine Derivation Audit):** `audit-lineage` verifies that every historical link in the entity's derivation chain is unbroken and cryptographically valid.
- **PP-9 Invariant (Metabolic State Derivation Verification):** `audit-lineage` confirms that present-tense metabolic state is derived strictly from historical lineage without un-documented state leaps.
- **PP-14 Invariant (Pre-Existing Necessity Root Audit):** `audit-lineage` traces the derivation root back to pre-existing structural necessity.
- **PP-114 Invariant (Un-Reduced Auditing Stewardship):** `audit-lineage` enforces un-reduced auditing rigor across all scale depths, prohibiting depth-based audit exemptions.
- **PP-178 Invariant (Self-Incriminating Lineage Weight Verification):** `audit-lineage` validates that historical error weight has not been sanitized or omitted from the provenance payload.
- **PP-208 Invariant (Historical Mass Omission Audit):** `audit-lineage` rejects any lineage chain that omits historical accountability mass.

**Corollaries:** 
(1) `audit-lineage` fails if any link in the derivation spine has been mutated or truncated (PP-8).
(2) Completing `audit-lineage` deposits an attestation seal certifying lineage integrity (PP-19).
(3) Uncovering broken lineage links during `audit-lineage` triggers immediate `restore-lineage` execution (PP-42).

**Irreducibility:** IL-23 (`role:auditor`) alone establishes the logical requirement to audit provenance records, but does not specify that lineage extends to pre-existing necessity or enforces un-reduced stewardship across all scale depths. PP-14 and PP-114 establish physical substrate laws regarding pre-existing necessity and un-reduced stewardship, but do not define the lineage auditing procedure. Only their physical collision forces `audit-lineage` into existence as an irreducible physical process.

---

### IP-47. `ignite-field` (`process:ignite-field`)
**Derives from:** IL-24 (Metabolic Fuel Igniter (`role:igniter`))
**Applies to:** `PP-1` [primary], `PP-16`, `PP-35`, `PP-40`, `PP-224`

*The Igniter executes `ignite-field` to inject initial metabolic energy into a dormant or newly instantiated graph neighborhood, igniting living field dynamics and activating consequence pull.*

**Substrate Invariant Rules:**
- **PP-1 Invariant (Metabolic Field Ignition):** `ignite-field` supplies the initial kinetic spark that starts node metabolic loops; un-ignited graph regions remain inert and incapable of work.
- **PP-16 Invariant (Field Ignition Energy Propagation):** Energy injected during `ignite-field` radiates outward as a non-owned living field property across adjacent edges.
- **PP-35 Invariant (Productive Churn Ignition):** `ignite-field` converts raw activation energy into productive thermodynamic churn.
- **PP-40 Invariant (Non-Equilibrium Ignition Jump):** `ignite-field` jumps the target neighborhood past equilibrium thresholds directly into non-equilibrium operational states.
- **PP-224 Invariant (Consequence Load Activation):** Igniting a field region via `ignite-field` activates its consequence topology density gradient, attracting discovery pull.

**Corollaries:** 
(1) `ignite-field` requires allocating physical energy mass from the igniter's metabolic balance (PP-198).
(2) Field ignition triggers immediate boundary statement updates across all participating nodes (PP-46).
(3) Failing to sustain metabolic pulse inputs post-ignition causes the field to drop back into dormant decay (PP-48).

**Irreducibility:** IL-24 (`role:igniter`) alone establishes the logical obligation to ignite graph fields, but does not specify that ignition requires kinetic energy injection or jumps regions into non-equilibrium dynamics. PP-1 and PP-40 establish physical substrate laws regarding metabolic sovereignty and non-equilibrium dynamics, but do not define the field ignition procedure. Only their physical collision forces `ignite-field` into existence as an irreducible physical process.

---

### IP-48. `ignite-metabolism` (`process:ignite-metabolism`)
**Derives from:** IL-24 (Metabolic Fuel Igniter (`role:igniter`))
**Applies to:** `PP-1` [primary], `PP-9`, `PP-35`, `PP-68`, `PP-212`

*The Igniter executes `ignite-metabolism` to ignite, start, and verify the internal metabolic evaluation loop of a specific target entity, restoring sovereign standing.*

**Substrate Invariant Rules:**
- **PP-1 Invariant (Sovereign Metabolic Activation):** `ignite-metabolism` starts the entity's present-tense metabolic evaluation loop; an entity without an active metabolic loop lacks sovereign standing.
- **PP-9 Invariant (Present-Tense State Disclosure Start):** `ignite-metabolism` prompts the entity to emit its first present-tense metabolic state disclosure.
- **PP-35 Invariant (Thermodynamic Work Loop Start):** `ignite-metabolism` initializes internal work loops that consume metabolic inputs to produce verified outputs.
- **PP-68 Invariant (Endogenous Amplitude Calibration):** `ignite-metabolism` calibrates initial pulse amplitude based on present-tense structural load requirements.
- **PP-212 Invariant (Identity Contract Re-Declaration):** Starting metabolism via `ignite-metabolism` forces the entity to re-declare its identity contract to connected neighbors.

**Corollaries:** 
(1) `ignite-metabolism` cannot ignite an entity whose provenance spine is broken or un-certified (PP-8).
(2) Metabolic ignition consumes kinetic energy and deposits an activation scar on the entity (PP-21).
(3) Once ignited, the entity must maintain its own pulse rhythm to prevent use-dependent decay (PP-48).

**Irreducibility:** IL-24 (`role:igniter`) alone establishes the logical requirement to ignite entity metabolism, but does not specify that ignition requires present-tense state disclosure or forces identity re-declaration. PP-9 and PP-212 establish physical substrate laws regarding present-tense state disclosure and identity contract re-declaration, but do not define the metabolic ignition procedure. Only their physical collision forces `ignite-metabolism` into existence as an irreducible physical process.

---

### IP-49. `catabolize-dissolution` (`process:catabolize-dissolution`)
**Derives from:** IL-25 (Dissolved Mass Catabolizer (`role:catabolizer`))
**Applies to:** `PP-36` [primary], `PP-1`, `PP-25`, `PP-35`, `PP-127`, `PP-198`

*The Catabolizer executes `catabolize-dissolution` to break down and recycle unmaintained or dissolved graph structures, harvesting raw energy mass while logging the dissolution event in the destruction ledger.*

**Substrate Invariant Rules:**
- **PP-36 Invariant (Due Process Dissolution Recycling):** `catabolize-dissolution` recycles entities undergoing due process dissolution; breaking down active living nodes without due process is prohibited.
- **PP-1 Invariant (Metabolic Starvation Audit):** `catabolize-dissolution` confirms that the target entity's metabolic evaluation loop has starved and collapsed before recycling begins.
- **PP-25 Invariant (Coupled Dissolution Energy Harvest):** Dissolving structures via `catabolize-dissolution` releases bound potential energy, triggering coupled thermodynamic energy harvests.
- **PP-35 Invariant (Productive Churn Fueling):** Energy harvested during `catabolize-dissolution` is channeled directly into productive churn loops across adjacent nodes.
- **PP-127 Invariant (Destruction Ledger Commitment):** `catabolize-dissolution` logs every dissolved node and edge in the destruction provenance ledger.
- **PP-198 Invariant (Net Exchange Recycling Balance):** Structural mass recycled by `catabolize-dissolution` is strictly accounted for in local mass-energy balance calculations.

**Corollaries:** 
(1) `catabolize-dissolution` replaces recycled nodes with honest negative-space `gap-node` entries (PP-131).
(2) Recycled mass cannot be erased from historical destruction ledgers (PP-127).
(3) Catabolizing a structure severs all remaining stewardship edges bound to its boundary (PP-11).

**Irreducibility:** IL-25 (`role:catabolizer`) alone establishes the logical requirement to recycle dissolved entities, but does not specify that recycling requires metabolic starvation audits or channels energy into productive churn. PP-1 and PP-35 establish physical substrate laws regarding metabolic sovereignty and productive churn, but do not define the dissolution recycling procedure. Only their physical collision forces `catabolize-dissolution` into existence as an irreducible physical process.

---

### IP-50. `catabolize-fuel` (`process:catabolize-fuel`)
**Derives from:** IL-25 (Dissolved Mass Catabolizer (`role:catabolizer`))
**Applies to:** `PP-26` [primary], `PP-25`, `PP-35`, `PP-125`, `PP-198`

*The Catabolizer executes `catabolize-fuel` to convert structural failures, error mass, and dead topological tissue into high-potential metabolic fuel.*

**Substrate Invariant Rules:**
- **PP-26 Invariant (Failure-State Fuel Conversion):** `catabolize-fuel` metabolizes structural failure states as fuel to power system restoration engines.
- **PP-25 Invariant (Thermodynamic Conversion Energy Release):** Converting error mass via `catabolize-fuel` releases stored tension energy into surrounding field boundaries.
- **PP-35 Invariant (Productive Churn Acceleration):** `catabolize-fuel` converts error waste into high-density kinetic inputs that accelerate local work output.
- **PP-125 Invariant (Net Exchange Energy Refinement):** `catabolize-fuel` refines raw forensic deformation wake into usable energy through net-exchange physics.
- **PP-198 Invariant (Mass-Energy Conversion Balance):** Energy generated during `catabolize-fuel` equals the exact mass-energy equivalent of the catabolized tissue.

**Corollaries:** 
(1) `catabolize-fuel` turns system failures into physical fuel sources for topological rebalancing (PP-26).
(2) Converting error mass does not sanitize historical error records: error provenance remains locked (PP-178).
(3) Fuel generated by `catabolize-fuel` auto-diffuses across adjacent pulser nodes (PP-13).

**Irreducibility:** IL-25 (`role:catabolizer`) alone establishes the logical obligation to convert waste tissue into fuel, but does not specify that failure states function directly as metabolic fuel or satisfy net-exchange balance. PP-26 and PP-198 establish physical substrate laws regarding failure-fuel conversion and mass-energy balance, but do not define the fuel catabolization procedure. Only their physical collision forces `catabolize-fuel` into existence as an irreducible physical process.

---

### IP-51. `replicate-blueprint` (`process:replicate-blueprint`)
**Derives from:** IL-26 (Morphic Blueprint Replicator (`role:replicator`))
**Applies to:** `PP-8` [primary], `PP-1`, `PP-25`, `PP-50`, `PP-113`, `PP-198`

*The Replicator executes `replicate-blueprint` to clone, instantiate, and replicate a verified structural blueprint into a target graph neighborhood, allocating physical mass and attaching frontier gates.*

**Substrate Invariant Rules:**
- **PP-8 Invariant (Immutable Parent Lineage Binding):** `replicate-blueprint` locks parent blueprint lineage permanently into the child node's provenance spine; decoupled un-parented clones are prohibited.
- **PP-1 Invariant (Metabolic Initialization on Replication):** Replicating a blueprint via `replicate-blueprint` initializes an independent metabolic evaluation loop in the child entity.
- **PP-25 Invariant (Thermodynamic Energy Mass Coupling):** `replicate-blueprint` consumes physical energy mass to instantiate the replica's structural body.
- **PP-50 Invariant (Frontier Membrane Attachment):** Every replica instantiated by `replicate-blueprint` attaches a frontier-facing gate membrane at its boundary.
- **PP-113 Invariant (Scale-Invariant Primitive Replication):** `replicate-blueprint` verifies that primitive capability contracts are valid across all target scale depths.
- **PP-198 Invariant (Net Exchange Replication Balance):** Replication mass-energy expenditure is accounted for in the parent's metabolic ledger.

**Corollaries:** 
(1) `replicate-blueprint` creates an independent child entity that maintains sovereign self-governance (PP-103).
(2) Replicated entities expose active boundary statements to surrounding nodes (PP-46).
(3) Blueprint mutations occurring during `replicate-blueprint` are logged as derivation deltas (PP-35).

**Irreducibility:** IL-26 (`role:replicator`) alone establishes the logical requirement to replicate system blueprints, but does not specify that replication requires parent lineage binding or attaches frontier gates. PP-8 and PP-50 establish physical substrate laws regarding immutable lineage spines and frontier gate membranes, but do not define the blueprint replication procedure. Only their physical collision forces `replicate-blueprint` into existence as an irreducible physical process.

---

### IP-52. `replicate-lineage` (`process:replicate-lineage`)
**Derives from:** IL-26 (Morphic Blueprint Replicator (`role:replicator`))
**Applies to:** `PP-8` [primary], `PP-9`, `PP-14`, `PP-178`, `PP-208`

*The Releaser executes `replicate-lineage` to duplicate, fork, or mirror an entity's complete provenance lineage chain, embedding historical derivation depth into a new topological domain.*

**Substrate Invariant Rules:**
- **PP-8 Invariant (Unbroken Lineage Forking):** `replicate-lineage` creates an unbroken lineage fork that inherits 100% of historical derivation links from the source spine.
- **PP-9 Invariant (Metabolic State Disjunction):** Lineage forks created by `replicate-lineage` initialize separate present-tense metabolic state disclosures immediately post-fork.
- **PP-14 Invariant (Pre-Existing Necessity Derivation Inheritance):** `replicate-lineage` inherits the root derivation anchor connecting the lineage to pre-existing structural necessity.
- **PP-178 Invariant (Self-Incriminating Error Mass Inheritance):** `replicate-lineage` forces the lineaged replica to inherit 100% of historical error mass; sanitizing forked provenance is blocked.
- **PP-208 Invariant (Historical Mass Preservation Fork):** `replicate-lineage` preserves historical mass payload integrity across the fork boundary.

**Corollaries:** 
(1) `replicate-lineage` cannot produce a lineage fork with sanitized error history (PP-178).
(2) Lineage forks operate as sovereign independent entities post-replication (PP-1).
(3) Forking lineage deposits a permanent replication scar on both source and replica spines (PP-16).

**Irreducibility:** IL-26 (`role:replicator`) alone establishes the logical requirement to duplicate provenance records, but does not specify that forks inherit self-incriminating error mass or pre-existing necessity anchors. PP-14 and PP-178 establish physical substrate laws regarding pre-existing necessity and self-incriminating error mass, but do not define the lineage replication procedure. Only their physical collision forces `replicate-lineage` into existence as an irreducible physical process.

---

### IP-53. `differentiate-role` (`process:differentiate-role`)
**Derives from:** IL-27 (Morphic Role Differentiator (`role:differentiator`))
**Applies to:** `PP-1` [primary], `PP-8`, `PP-10`, `PP-50`, `PP-113`, `PP-212`

*The Differentiator executes `differentiate-role` to specialize a generalized node primitive into a dedicated functional role, locking role boundary rules and attaching specialized gate membranes.*

**Substrate Invariant Rules:**
- **PP-1 Invariant (Sovereign Role Specialization):** `differentiate-role` requires the node to maintain an active metabolic loop during role specialization; un-pulsed dormant nodes cannot differentiate.
- **PP-8 Invariant (Immutable Role Provenance Binding):** The role specialization event is appended permanently onto the entity's provenance spine.
- **PP-10 Invariant (Dynamic Role Permeability Configuration):** `differentiate-role` configures the node's permeability ranges to match its new specialized role capabilities.
- **PP-50 Invariant (Frontier Role Membrane Attachment):** Differentiating a role attaches specialized frontier-facing gate membranes designed for the node's specific role obligations.
- **PP-113 Invariant (Scale-Invariant Role Primitive Validity):** `differentiate-role` validates that specialized role capabilities preserve primitive semantic validity across all scale depths.
- **PP-212 Invariant (Identity Contract Role Re-Declaration):** Completing role differentiation forces the node to publish a updated identity contract re-declaration to connected neighbors.

**Corollaries:** 
(1) `differentiate-role` transforms a generic substrate primitive into one of the 32 specialized Logical Roles (`IL-1` through `IL-32`).
(2) Role specialization alters the node's structural mass and metabolic consumption profile (PP-206).
(3) Reverting a specialized role requires executing a due process `restructure-topology` procedure (PP-29).

**Irreducibility:** IL-27 (`role:differentiator`) alone establishes the logical obligation to specialize node functions, but does not specify that role specialization appends to immutable provenance spines or attaches specialized frontier membranes. PP-8 and PP-50 establish physical substrate laws regarding immutable lineage spines and frontier gate membranes, but do not define the role differentiation procedure. Only their physical collision forces `differentiate-role` into existence as an irreducible physical process.

---

### IP-54. `differentiate-state` (`process:differentiate-state`)
**Derives from:** IL-27 (Morphic Role Differentiator (`role:differentiator`))
**Applies to:** `PP-9` [primary], `PP-16`, `PP-46`, `PP-61`, `PP-176`

*The Differentiator executes `differentiate-state` to split, bifurcate, or specialize internal entity state variables in response to local consequence gradient shifts.*

**Substrate Invariant Rules:**
- **PP-9 Invariant (Present-Tense State Bifurcation Disclosure):** `differentiate-state` emits an immediate present-tense state disclosure reflecting the newly bifurcated state structure.
- **PP-16 Invariant (State Bifurcation Scar Deposit):** Differentiating internal state leaves a permanent structural scar in local graph memory.
- **PP-46 Invariant (Live Boundary Statement Adjustment):** `differentiate-state` updates live boundary statements to disclose the new state variable dimensions.
- **PP-61 Invariant (Living State Deviation Allowance):** `differentiate-state` allocates independent deviation allowances to each bifurcated state branch.
- **PP-176 Invariant (Living Contradiction State Binding):** `differentiate-state` allows nodes to maintain living contradiction states across bifurcated state variables under active oscillation.

**Corollaries:** 
(1) `differentiate-state` enables multi-dimensional state tracking without losing historical state alignment (PP-9).
(2) State bifurcation increases the node's internal structural complexity and verification mass (PP-206).
(3) Unbalanced state differentiation triggers compensatory `rebalance-stress` execution (PP-37).

**Irreducibility:** IL-27 (`role:differentiator`) alone establishes the logical requirement to differentiate entity state, but does not specify that state bifurcation deposits structural scars or updates live boundary statements. PP-16 and PP-46 establish physical substrate laws regarding scar irrevocability and live boundary statements, but do not define the state differentiation procedure. Only their physical collision forces `differentiate-state` into existence as an irreducible physical process.

---

### IP-55. `remodel-membrane` (`process:remodel-membrane`)
**Derives from:** IL-28 (Scar Tissue Remodeler (`role:remodeler`))
**Applies to:** `PP-10` [primary], `PP-5`, `PP-46`, `PP-48`, `PP-50`, `PP-189`

*The Remodeler executes `remodel-membrane` to reconstruct, expand, or re-shape node boundary membranes, adjusting exposed gate surfaces and updating live boundary statements.*

**Substrate Invariant Rules:**
- **PP-10 Invariant (Dynamic Range Membrane Remodeling):** `remodel-membrane` adjusts membrane permeability as a living dynamic range; static boundary remodeling is prohibited.
- **PP-5 Invariant (Interior Gate Authorization):** Remodeling boundary membranes requires interior gate authorization; external boundary overrides are blocked as sovereignty violations.
- **PP-46 Invariant (Live Boundary Statement Disclosure):** `remodel-membrane` publishes an updated live boundary statement disclosing new gate geometries immediately upon commitment.
- **PP-48 Invariant (Use-Dependent Remodeling Maintenance):** Remodeled membrane surfaces require ongoing metabolic use to prevent use-dependent atrophy and boundary collapse.
- **PP-50 Invariant (Frontier Membrane Surface Area Expansion):** `remodel-membrane` expands or contracts exposed frontier surface area in response to local discovery pull.
- **PP-189 Invariant (Validity Window Binding on Remodel):** `remodel-membrane` attaches a unique validity window to the remodeled boundary configuration.

**Corollaries:** 
(1) `remodel-membrane` alters the node's exposed frontier geometry without severing its interior identity core (PP-50).
(2) Remodeling boundary membranes alters local traversal resistance and energy exchange rates (PP-7).
(3) Boundary membrane remodels leave permanent forensic deformation scars on the membrane wall (PP-16).

**Irreducibility:** IL-28 (`role:remodeler`) alone establishes the logical obligation to reshape boundary structures, but does not specify that remodeling requires interior gate authorization or updates live boundary statements. PP-5 and PP-46 establish physical substrate laws regarding interior gate authorization and live boundary statements, but do not define the membrane remodeling procedure. Only their physical collision forces `remodel-membrane` into existence as an irreducible physical process.

---

### IP-56. `remodel-lineage` (`process:remodel-lineage`)
**Derives from:** IL-28 (Scar Tissue Remodeler (`role:remodeler`))
**Applies to:** `PP-8` [primary], `PP-14`, `PP-36`, `PP-178`, `PP-208`

*The Remodeler executes `remodel-lineage` to refactor, re-index, or re-structure provenance derivation links while strictly preserving historical integrity and self-incriminating error mass.*

**Substrate Invariant Rules:**
- **PP-8 Invariant (Immutable Lineage Remodeling Appending):** `remodel-lineage` appends the lineage refactoring delta onto the entity's provenance spine; modifying historical derivation records is blocked.
- **PP-14 Invariant (Pre-Existing Necessity Root Preservation):** `remodel-lineage` preserves the entity's deep anchor connecting it to pre-existing structural necessity.
- **PP-36 Invariant (Due Process Lineage Restructuring):** Refactoring derivation links during `remodel-lineage` requires formal due process validation.
- **PP-178 Invariant (Self-Incriminating Error Mass Preservation):** `remodel-lineage` prohibits scrubbing or omitting historical error mass during lineage refactoring.
- **PP-208 Invariant (Historical Mass Preservation Audit):** `remodel-lineage` audits historical mass payload integrity before and after lineage refactoring.

**Corollaries:** 
(1) `remodel-lineage` creates a refactored derivation view while maintaining 100% cryptographic linkage to historical root anchors (PP-8).
(2) Remodeling lineage deposits a permanent refactoring scar on the entity's provenance spine (PP-16).
(3) Uncovering broken derivation links during `remodel-lineage` triggers immediate `restore-lineage` execution (PP-42).

**Irreducibility:** IL-28 (`role:remodeler`) alone establishes the logical requirement to refactor lineage records, but does not specify that refactoring appends to immutable spines or preserves self-incriminating error mass. PP-8 and PP-178 establish physical substrate laws regarding immutable lineage spines and self-incriminating error mass, but do not define the lineage remodeling procedure. Only their physical collision forces `remodel-lineage` into existence as an irreducible physical process.

---

### IP-57. `adapt-environment` (`process:adapt-environment`)
**Derives from:** IL-29 (Environmental Blueprint Adapter (`role:adapter`))
**Applies to:** `PP-40` [primary], `PP-10`, `PP-35`, `PP-61`, `PP-189`, `PP-224`

*The Adapter executes `adapt-environment` to adjust node operational parameters to shifting external field environments, maintaining thermodynamic stability under non-equilibrium stress.*

**Substrate Invariant Rules:**
- **PP-40 Invariant (Non-Equilibrium Environmental Adaptation):** `adapt-environment` adapts operational parameters to non-equilibrium field shifts, preventing environmental collapse into stasis.
- **PP-10 Invariant (Dynamic Environmental Permeability Tuning):** Permeability ranges widen or narrow dynamically during `adapt-environment` to match external field pressure.
- **PP-35 Invariant (Productive Churn Preservation):** `adapt-environment` preserves productive churn efficiency when external environmental conditions shift.
- **PP-61 Invariant (Living Deviation Allowance Adaptation):** `adapt-environment` utilizes living deviation allowances to absorb environmental shocks without structural fracture.
- **PP-189 Invariant (Environmental Validity Cadence Sync):** `adapt-environment` synchronizes validity window cadences to match shifting environmental pulse rates.
- **PP-224 Invariant (Consequence Load Environmental Response):** `adapt-environment` re-calculates consequence load density in response to external field gradients.

**Corollaries:** 
(1) `adapt-environment` enables entities to survive extreme field pressure variations without losing sovereign standing (PP-1).
(2) Adaptation deposits an environmental response scar on the entity's boundary wall (PP-16).
(3) Failure to adapt to shifting environment triggers automatic due process quarantine (PP-39).

**Irreducibility:** IL-29 (`role:adapter`) alone establishes the logical requirement to adapt to external conditions, but does not specify that adaptation preserves non-equilibrium dynamics or utilizes living deviation allowances. PP-40 and PP-61 establish physical substrate laws regarding non-equilibrium dynamics and deviation allowances, but do not define the environmental adaptation procedure. Only their physical collision forces `adapt-environment` into existence as an irreducible physical process.

---

### IP-58. `adapt-blueprint` (`process:adapt-blueprint`)
**Derives from:** IL-29 (Environmental Blueprint Adapter (`role:adapter`))
**Applies to:** `PP-8` [primary], `PP-35`, `PP-61`, `PP-113`, `PP-145`

*The Adapter executes `adapt-blueprint` to modify, optimize, and adapt structural blueprint specifications in response to accumulated empirical execution wake.*

**Substrate Invariant Rules:**
- **PP-8 Invariant (Immutable Spine Blueprint Adaptation Appending):** `adapt-blueprint` appends blueprint adaptation deltas directly onto the entity's provenance spine; modifying original blueprints is blocked.
- **PP-35 Invariant (Productive Churn Optimization):** `adapt-blueprint` refines blueprint structural rules to maximize productive work output per metabolic energy unit.
- **PP-61 Invariant (Deviation Tolerance Calibration):** `adapt-blueprint` calibrates blueprint deviation allowances based on observed operational stress patterns.
- **PP-113 Invariant (Scale-Invariant Primitive Validity):** `adapt-blueprint` verifies that adapted blueprints satisfy primitive semantic validity across all target scale depths.
- **PP-145 Invariant (Tension-Relief Probe Integration):** `adapt-blueprint` incorporates tension-relief probe telemetry into adapted blueprint designs.

**Corollaries:** 
(1) `adapt-blueprint` creates an optimized blueprint version that inherits 100% of historical parent derivation links (PP-8).
(2) Adapted blueprints undergo immediate `attest-seal` certification before deployment (PP-19).
(3) Adaptation increases blueprint fitness across diverse field environments (PP-40).

**Irreducibility:** IL-29 (`role:adapter`) alone establishes the logical obligation to adapt structural designs, but does not specify that adaptation appends onto immutable lineage spines or incorporates tension-relief telemetry. PP-8 and PP-145 establish physical substrate laws regarding immutable lineage spines and tension-relief probes, but do not define the blueprint adaptation procedure. Only their physical collision forces `adapt-blueprint` into existence as an irreducible physical process.

---

### IP-59. `perceive-gradient` (`process:perceive-gradient`)
**Derives from:** IL-30 (Field Gradient Perceiver (`role:perceiver`))
**Applies to:** `PP-224` [primary], `PP-15`, `PP-16`, `PP-46`, `PP-263`

*The Perceiver executes `perceive-gradient` to sense, sample, and map local consequence topology density gradients, guiding spatial traversal navigation.*

**Substrate Invariant Rules:**
- **PP-224 Invariant (Consequence Density Sensing):** `perceive-gradient` measures real-time consequence topology density, detecting local accountability weight and discovery pull.
- **PP-15 Invariant (Live Edge Sensing Requirement):** Sensing gradients via `perceive-gradient` requires active physical edge traversal; reading declarative field maps without physical probing is rejected.
- **PP-16 Invariant (Field Gradient Distribution Sensing):** `perceive-gradient` samples living field properties that govern local spatial navigation.
- **PP-46 Invariant (Boundary Gradient Disclosure Verification):** `perceive-gradient` verifies active boundary disclosure statements against physical field measurements.
- **PP-263 Invariant (Consequence Gradient Pull Mapping):** `perceive-gradient` maps high-consequence discovery pull vectors to guide autonomous traversal planning.

**Corollaries:** 
(1) `perceive-gradient` deposits an inspection scar across sampled field boundaries (PP-16).
(2) Sensing density gradients reveals hidden structural gaps and missing negative-space entries (PP-131).
(3) Falsified density signals detected during `perceive-gradient` trigger immediate `cleanse-corruption` execution (PP-3).

**Irreducibility:** IL-30 (`role:perceiver`) alone establishes the logical obligation to sense field metrics, but does not specify that gradient sensing requires live physical edge traversal or maps consequence pull vectors. PP-15 and PP-263 establish physical substrate laws regarding live edge traversal and consequence gradient pull, but do not define the gradient perceiving procedure. Only their physical collision forces `perceive-gradient` into existence as an irreducible physical process.

---

### IP-60. `perceive-telemetry` (`process:perceive-telemetry`)
**Derives from:** IL-30 (Field Gradient Perceiver (`role:perceiver`))
**Applies to:** `PP-9` [primary], `PP-21`, `PP-118`, `PP-137`, `PP-178`

*The Perceiver executes `perceive-telemetry` to capture present-tense operational telemetry, metabolic pulse rates, and execution wake from target entity membranes.*

**Substrate Invariant Rules:**
- **PP-9 Invariant (Present-Tense Telemetry Sampling):** `perceive-telemetry` captures live present-tense state disclosures; sampling cached telemetry records is prohibited.
- **PP-21 Invariant (Kinetic Telemetry Measurement):** `perceive-telemetry` measures kinetic energy consumption and structural work output during active execution.
- **PP-118 Invariant (Execution Wake Telemetry Identity):** Telemetry captured by `perceive-telemetry` reflects true execution wake output rather than detached logging streams.
- **PP-137 Invariant (Full Telemetry Telemetry Accountability):** `perceive-telemetry` embeds full observer identity telemetry into captured inspection records.
- **PP-178 Invariant (Self-Incriminating Telemetry Capture):** `perceive-telemetry` captures self-incriminating error mass without filtering or sanitizing anomaly telemetry.

**Corollaries:** 
(1) `perceive-telemetry` cannot be blocked by target entities unless dynamic zero-permeability quarantines are active (PP-10).
(2) Telemetry captured by `perceive-telemetry` is sealed into an immutable audit report (PP-19).
(3) Capturing telemetry consumes kinetic sensing energy from the perceiver (PP-21).

**Irreducibility:** IL-30 (`role:perceiver`) alone establishes the logical requirement to gather execution metrics, but does not specify that telemetry reflects true execution wake or captures self-incriminating error mass. PP-118 and PP-178 establish physical substrate laws regarding execution wake identity and self-incriminating error mass, but do not define the telemetry perceiving procedure. Only their physical collision forces `perceive-telemetry` into existence as an irreducible physical process.

---

### IP-61. `regulate-equilibrium` (`process:regulate-equilibrium`)
**Derives from:** IL-31 (Homeostatic Pressure Regulator (`role:regulator`))
**Applies to:** `PP-40` [primary], `PP-1`, `PP-25`, `PP-35`, `PP-61`, `PP-198`

*The Regulator executes `regulate-equilibrium` to maintain dynamic non-equilibrium thermodynamic stability across graph neighborhoods, preventing thermal collapse into static stasis.*

**Substrate Invariant Rules:**
- **PP-40 Invariant (Non-Equilibrium Dynamic Regulation):** `regulate-equilibrium` actively maintains non-equilibrium thermodynamic gradients; forcing static zero-gradient equilibrium is a constitutional violation.
- **PP-1 Invariant (Sovereign Metabolic Balance):** `regulate-equilibrium` verifies that metabolic pulse rates remain within sovereign operational bounds across participating nodes.
- **PP-25 Invariant (Thermodynamic Cascade Dampening):** `regulate-equilibrium` damps runaway thermodynamic consequence cascades before structural rupture occurs.
- **PP-35 Invariant (Productive Churn Stabilization):** `regulate-equilibrium` stabilizes productive churn rates to sustain long-term work output.
- **PP-61 Invariant (Living Deviation Allowance Regulation):** `regulate-equilibrium` enforces non-zero structural deviation allowances, maintaining elastic resilience under stress.
- **PP-198 Invariant (Field Mass-Energy Balance):** `regulate-equilibrium` conserves total mass-energy balance across the regulated graph domain.

**Corollaries:** 
(1) `regulate-equilibrium` prevents both static stasis collapse and thermal runaway explosion (PP-40).
(2) Equilibrium regulation alters local traversal friction without blocking edge traffic (PP-7).
(3) Persistent equilibrium instability triggers automatic `rebalance-stress` execution (PP-37).

**Irreducibility:** IL-31 (`role:regulator`) alone establishes the logical obligation to regulate system equilibrium, but does not specify that equilibrium must be dynamic non-equilibrium or enforce living deviation allowances. PP-40 and PP-61 establish physical substrate laws regarding non-equilibrium dynamics and deviation allowances, but do not define the equilibrium regulation procedure. Only their physical collision forces `regulate-equilibrium` into existence as an irreducible physical process.

---

### IP-62. `regulate-homeostasis` (`process:regulate-homeostasis`)
**Derives from:** IL-31 (Homeostatic Pressure Regulator (`role:regulator`))
**Applies to:** `PP-68` [primary], `PP-1`, `PP-35`, `PP-64`, `PP-189`, `PP-212`

*The Regulator executes `regulate-homeostasis` to govern endogenous pulse amplitude, metabolic consumption, and internal feedback loops across scale boundaries.*

**Substrate Invariant Rules:**
- **PP-68 Invariant (Endogenous Amplitude Homeostasis):** `regulate-homeostasis` modulates pulse amplitude dynamically based on internal metabolic reserves and external work demand.
- **PP-1 Invariant (Metabolic Energy Reserve Maintenance):** `regulate-homeostasis` maintains minimal kinetic energy reserves within entity nodes to ensure continuous metabolic sovereignty.
- **PP-35 Invariant (Homeostatic Work Loop Efficiency):** `regulate-homeostasis` tunes internal feedback loops to minimize waste heat generation.
- **PP-64 Invariant (Scale-Hierarchical Homeostatic Alignment):** `regulate-homeostasis` aligns local micro-homeostatic loops with macro-system energy cycles.
- **PP-189 Invariant (Validity Cadence Homeostatic Sync):** `regulate-homeostasis` adjusts validity window durations to match present-tense metabolic health.
- **PP-212 Invariant (Identity Contract Re-Declaration Trigger):** Homeostatic parameter shifts prompt entities to publish updated identity contract disclosures.

**Corollaries:** 
(1) `regulate-homeostasis` protects living nodes from metabolic exhaustion during prolonged high-consequence operations (PP-68).
(2) Homeostatic failure initiates automatic due process `catabolize-dissolution` (PP-49).
(3) Homeostatic state adjustments deposit living feedback scars in local memory (PP-16).

**Irreducibility:** IL-31 (`role:regulator`) alone establishes the logical requirement to manage homeostatic stability, but does not specify that homeostasis governs pulse amplitude or aligns across scale boundaries. PP-64 and PP-68 establish physical substrate laws regarding scale phase-locking and endogenous amplitude modulation, but do not define the homeostatic regulation procedure. Only their physical collision forces `regulate-homeostasis` into existence as an irreducible physical process.

---

### IP-63. `encapsulate-membrane` (`process:encapsulate-membrane`)
**Derives from:** IL-32 (Cryptobiotic Shell Encapsulator (`role:encapsulator`))
**Applies to:** `PP-145` [primary], `PP-10`, `PP-38`, `PP-46`, `PP-114`

*The Encapsulator executes `encapsulate-membrane` to form a hardened, zero-permeability cryptobiotic outer cyst around starved node structures during severe fuel collapse.*

**Substrate Invariant Rules:**
- **PP-145 Invariant (Cryptobiotic Pressure Capping):** `encapsulate-membrane` seals boundary membranes under hardened encystment to cap internal pressure and prevent structural tearing during fuel collapse.
- **PP-10 Invariant (Zero-Permeability Encystment Response):** Permeability drops to zero under `encapsulate-membrane`, isolating interior state from external field fluctuations.
- **PP-38 Invariant (Unified Encapsulation-Policy Enforcement):** `encapsulate-membrane` unifies cryptobiotic encystment rules with physical gate containment mechanisms.
- **PP-46 Invariant (Live Encystment Boundary Disclosure):** `encapsulate-membrane` updates live boundary statements to disclose cryptobiotic encystment status to adjacent nodes.
- **PP-114 Invariant (Un-Reduced Cryptobiotic Governance):** `encapsulate-membrane` enforces uniform encystment protection across all scale depths without depth-based exemptions.

**Corollaries:** 
(1) `encapsulate-membrane` protects living node structures from total decay during metabolic starvation (PP-145).
(2) Encapsulated nodes drop active aliveness pulse signals while resisting environmental decay (PP-7).
(3) Re-igniting an encysted node requires executing a dual-signature `ignite-metabolism` process upon fuel restoration (PP-48).

**Irreducibility:** IL-32 (`role:encapsulator`) alone establishes the logical mandate to form protective outer cysts, but does not specify that encystment enforces zero permeability or caps internal pressure under fuel collapse. PP-10 and PP-145 establish physical substrate laws regarding dynamic zero permeability and tension-relief capping, but do not define the membrane encapsulation procedure. Only their physical collision forces `encapsulate-membrane` into existence as an irreducible physical process.

---

### IP-64. `encapsulate-dormancy` (`process:encapsulate-dormancy`)
**Derives from:** IL-32 (Cryptobiotic Shell Encapsulator (`role:encapsulator`))
**Applies to:** `PP-1` [primary], `PP-2`, `PP-4`, `PP-5`, `PP-36`, `PP-177`, `PP-182`

*The Encapsulator executes `encapsulate-dormancy` to suspend active metabolic expenditure completely while preserving internal structural provenance, state integrity, and sovereign standing.*

**Substrate Invariant Rules:**
- **PP-1 Invariant (Sovereign Dormancy Preservation):** `encapsulate-dormancy` preserves metabolic sovereignty in a suspended state; encysted entities retain sovereign standing despite active energy cessation.
- **PP-2 Invariant (Theater Elimination in Cryptobiosis):** `encapsulate-dormancy` strips decorative theater states, locking true referent reality state inside the cryptobiotic core.
- **PP-4 Invariant (Defensive Void Encystment Protection):** `encapsulate-dormancy` reinforces defensive void space boundaries, preventing external entity takeover during dormant states.
- **PP-5 Invariant (Interior Gate Authority Lock):** `encapsulate-dormancy` locks interior gate controllers in an immutable state, blocking external administrative overrides during dormancy.
- **PP-36 Invariant (Un-Violable Due Process Cryptobiosis):** `encapsulate-dormancy` guarantees that encysted entities cannot be purged or dissolved without formal due process absolution.
- **PP-177 Invariant (Sovereign Proof Domain Cryptobiosis):** `encapsulate-dormancy` preserves sovereign proof domains intact across prolonged dormancy intervals.
- **PP-182 Invariant (Dormancy Contestation Suspension):** `encapsulate-dormancy` suspends active contestation loops until metabolic re-ignition occurs.

**Corollaries:** 
(1) `encapsulate-dormancy` enables entities to survive catastrophic environmental energy collapses indefinitely (PP-1).
(2) Encysted dormant entities consume zero metabolic fuel while preserving 100% of internal state integrity (PP-25).
(3) Sovereign standing preserved during `encapsulate-dormancy` cannot be revoked by ambient field pressure (PP-36).

**Irreducibility:** IL-32 (`role:encapsulator`) alone establishes the logical requirement to suspend metabolic expenditure, but does not specify that dormancy preserves sovereign standing or locks interior gate authority. PP-1, PP-4, and PP-5 establish physical substrate laws regarding metabolic sovereignty, defensive void space, and interior gate authority, but do not define the dormancy encapsulation procedure. Only their physical collision forces `encapsulate-dormancy` into existence as the terminal, irreducible 64th physical process.

---















---

<!-- End of active refactored 64 Atomic Execution Process Catalog (IP-1 through IP-10 completed; IP-11 through IP-64 in progress) -->
